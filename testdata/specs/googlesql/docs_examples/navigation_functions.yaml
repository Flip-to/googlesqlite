# Generated by TestDocsExamples from google/googlesql@d82db99 docs/navigation_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "navigation_functions#12 L872 PERCENTILE_DISC"
    sql: |-
      SELECT
        x,
        PERCENTILE_DISC(x, 0) OVER() AS min,
        PERCENTILE_DISC(x, 0.5) OVER() AS median,
        PERCENTILE_DISC(x, 1) OVER() AS max
      FROM UNNEST(['c', NULL, 'b', 'a']) AS x
    expected:
      rows:
        - ["c","a","b","c"]
        - [null,"a","b","c"]
        - ["b","a","b","c"]
        - ["a","a","b","c"]
      unordered: true
  - desc: "navigation_functions#13 L893 PERCENTILE_DISC"
    sql: |-
      SELECT
        x,
        PERCENTILE_DISC(x, 0 RESPECT NULLS) OVER() AS min,
        PERCENTILE_DISC(x, 0.5 RESPECT NULLS) OVER() AS median,
        PERCENTILE_DISC(x, 1 RESPECT NULLS) OVER() AS max
      FROM UNNEST(['c', NULL, 'b', 'a']) AS x
    expected:
      rows:
        - ["c",null,"a","c"]
        - [null,null,"a","c"]
        - ["b",null,"a","c"]
        - ["a",null,"a","c"]
      unordered: true
