# Generated by TestDocsExamples from google/googlesql@d82db99 docs/functions-reference.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "functions-reference#1 L112 Named arguments"
    sql: |-
      -- This doesn't work.
      SELECT token, count
      FROM CountTokensInText(
        ['baseball', 'football', 'tennis'],
        delimiters => ' .,!?()',
        paragraph => 'Would you prefer softball, baseball, or tennis? There is also swimming.')
    expected:
      error:
        contains: ""
  - desc: "functions-reference#2 L192 SAFE. prefix"
    sql: |-
      SELECT SAFE.SUBSTR('foo', 0, -2) AS safe_output UNION ALL
      SELECT SAFE.SUBSTR('bar', 0, 2) AS safe_output
    expected:
      rows:
        - [null]
        - ["ba"]
      unordered: true
