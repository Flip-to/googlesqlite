# Generated by TestDocsExamples from google/googlesql@d82db99 docs/graph-query-statements.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "graph-query-statements#15 L763 Examples"
    sql: |-
      -- Error: m doesn't exist
      GRAPH FinGraph
      MATCH (p:Person)-[o:Owns]->(a:Account)
      FILTER WHERE m.Id <> 1
      RETURN p.name
    expected:
      error:
        contains: ""
  - desc: "graph-query-statements#16 L774 Examples"
    sql: |-
      -- ERROR: date_of_birth isn't a property of p
      GRAPH FinGraph
      MATCH (p:Person)-[o:Owns]->(a:Account)
      FILTER WHERE p.date_of_birth < '1990-01-10'
      RETURN p.name
    expected:
      error:
        contains: ""
  - desc: "graph-query-statements#19 L912 Examples"
    sql: |-
      -- Error: Expected keyword OFFSET but got identifier "element"
      GRAPH FinGraph
      FOR element in [1,2,3]
      WITH element as col
      RETURN col
      ORDER BY col
    expected:
      error:
        contains: ""
  - desc: "graph-query-statements#46 L1802 Examples"
    sql: |-
      -- Error: You can't return a graph element
      GRAPH FinGraph
      MATCH (p:Person)
      RETURN p
    expected:
      error:
        contains: ""
  - desc: "graph-query-statements#61 L2152 Examples"
    sql: |-
      -- Error: src doesn't exist
      GRAPH FinGraph
      MATCH (src:Account)-[transfer:Transfers]->(dst:Account)
      WITH dst
      RETURN src.id AS source_id
    expected:
      error:
        contains: ""
  - desc: "graph-query-statements#64 L2309 Examples"
    sql: |-
      -- ERROR: GQL doesn't allow chaining EXCEPT DISTINCT with UNION ALL
      GRAPH FinGraph
      MATCH (p:Person)
      RETURN p.name, 1 AS group_id
      UNION ALL
      MATCH (p:Person)
      RETURN 2 AS group_id, p.name
      EXCEPT DISTINCT
      MATCH (p:Person)
      RETURN 3 AS group_id, p.name
    expected:
      error:
        contains: ""
