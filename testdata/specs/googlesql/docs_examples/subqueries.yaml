# Generated by TestDocsExamples from google/googlesql@d82db99 docs/subqueries.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "subqueries#5 L265 Quantified `LIKE` subqueries"
    skip: "not_in_bigquery: LIKE ANY (subquery); BigQuery raises \"The LIKE ANY|SOME|ALL operator does not support subquery expression as patterns\"."
    sql: |-
      WITH Words AS (
        SELECT '%orb%' as chars UNION ALL
        SELECT 'xyg'
      )
      SELECT 'corba' LIKE ANY (SELECT chars FROM Words) as result
    expected:
      rows:
        - ["TRUE"]
