# Generated by TestDocsExamples from google/googlesql@d82db99 docs/query-syntax.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#1 L150 SELECT *"
    sql: |-
      SELECT * FROM (SELECT "apple" AS fruit, "carrot" AS vegetable)
    expected:
      rows:
        - ["apple","carrot"]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#2 L185 SELECT expression.*"
    sql: |-
      WITH groceries AS
        (SELECT "milk" AS dairy,
         "eggs" AS protein,
         "bread" AS grain)
      SELECT g.*
      FROM groceries AS g
    expected:
      rows:
        - ["milk","eggs","bread"]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#3 L202 SELECT expression.*"
    sql: |-
      WITH locations AS
        (SELECT STRUCT("Seattle" AS city, "Washington" AS state) AS location
        UNION ALL
        SELECT STRUCT("Phoenix" AS city, "Arizona" AS state) AS location)
      SELECT l.location.*
      FROM locations l
    expected:
      rows:
        - ["Seattle","Washington"]
        - ["Phoenix","Arizona"]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#4 L218 SELECT expression.*"
    sql: |-
      WITH locations AS
        (SELECT ARRAY<STRUCT<city STRING, state STRING>>[("Seattle", "Washington"),
          ("Phoenix", "Arizona")] AS location)
      SELECT l.LOCATION[offset(0)].*
      FROM locations l
    expected:
      rows:
        - ["Seattle","Washington"]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#5 L237 SELECT * EXCEPT"
    sql: |-
      WITH orders AS
        (SELECT 5 as order_id,
        "sprocket" as item_name,
        200 as quantity)
      SELECT * EXCEPT (order_id)
      FROM orders
    expected:
      rows:
        - ["sprocket",200]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#6 L267 SELECT * REPLACE"
    sql: |-
      WITH orders AS
        (SELECT 5 as order_id,
        "sprocket" as item_name,
        200 as quantity)
      SELECT * REPLACE ("widget" AS item_name)
      FROM orders
    expected:
      rows:
        - [5,"widget",200]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#7 L279 SELECT * REPLACE"
    sql: |-
      WITH orders AS
        (SELECT 5 as order_id,
        "sprocket" as item_name,
        200 as quantity)
      SELECT * REPLACE (quantity/2 AS quantity)
      FROM orders
    expected:
      rows:
        - [5,"sprocket",100]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#8 L307 SELECT DISTINCT"
    sql: |-
      WITH PlayerStats AS (
        SELECT ['Coolidge', 'Adams'] as Name, 3 as PointsScored UNION ALL
        SELECT ['Adams', 'Buchanan'], 0 UNION ALL
        SELECT ['Coolidge', 'Adams'], 1 UNION ALL
        SELECT ['Kiran', 'Noam'], 1)
      SELECT DISTINCT Name
      FROM PlayerStats
    expected:
      rows:
        - [["Coolidge","Adams"]]
        - [["Adams","Buchanan"]]
        - [["Kiran","Noam"]]
      unordered: true
  - desc: "query-syntax#10 L661 TABLE` statement"
    skip: "not_in_bigquery: TABLE statement; BigQuery raises \"TABLE statements are not supported\"."
    sql: |-
      TABLE Roster
    expected:
      rows:
        - ["Adams","50"]
        - ["Buchanan","52"]
        - ["Coolidge","52"]
        - ["Davis","51"]
        - ["Eisenhower","77"]
      unordered: true
  - desc: "query-syntax#11 L678 TABLE` statement"
    skip: "not_in_bigquery: TABLE statement; BigQuery raises \"TABLE statements are not supported\"."
    sql: |-
      TABLE Roster ORDER BY LastName LIMIT 3
    expected:
      rows:
        - ["Adams","50"]
        - ["Buchanan","52"]
        - ["Coolidge","52"]
  - desc: "query-syntax#12 L693 TABLE` statement"
    skip: "not_in_bigquery: TABLE statement; BigQuery raises \"TABLE statements are not supported\"."
    setup:
      - |-
        CREATE TABLE PlayerStats AS SELECT 'Adams' as LastName, 51 as OpponentID, 3 as PointsScored UNION ALL
          SELECT 'Buchanan', 77, 0 UNION ALL
          SELECT 'Coolidge', 77, 1 UNION ALL
          SELECT 'Adams', 52, 4 UNION ALL
          SELECT 'Buchanan', 50, 13
    sql: |-
      WITH TopScorers AS (
        SELECT LastName, PointsScored
        FROM PlayerStats
        WHERE PointsScored > 10
      )
      TABLE TopScorers
    expected:
      rows:
        - ["Buchanan","13"]
  - desc: "query-syntax#13 L711 TABLE` statement"
    skip: "not_in_bigquery: TABLE statement; BigQuery raises \"TABLE statements are not supported\"."
    sql: |-
      TABLE CustomerRangeWithCustomerType(100, 200)
    expected:
      rows:
        - ["105","\"Active account\"","CUSTOMER_TYPE_ADVERTISER"]
        - ["180","\"Inactive account\"","CUSTOMER_TYPE_ADVERTISER"]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#14 L778 UNNEST` operator"
    sql: |-
      SELECT * FROM UNNEST ([10,20,30]) as numbers WITH OFFSET
    expected:
      rows:
        - [10,0]
        - [20,1]
        - [30,2]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#15 L810 UNNEST` and structs"
    sql: |-
      SELECT *
      FROM UNNEST(
        ARRAY<
          STRUCT<
            x INT64,
            y STRING,
            z STRUCT<a INT64, b INT64>>>[
              (1, 'foo', (10, 11)),
              (3, 'bar', (20, 21))])
    expected:
      rows:
        - [1,"foo",[10,11]]
        - [3,"bar",[20,21]]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#16 L838 UNNEST` and structs"
    sql: |-
      SELECT *, struct_value
      FROM UNNEST(
        ARRAY<
          STRUCT<
          x INT64,
          y STRING>>[
            (1, 'foo'),
            (3, 'bar')]) AS struct_value
    expected:
      rows:
        - [3,"bar",[3,"bar"]]
        - [1,"foo",[1,"foo"]]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#19 L997 Array subscript operator limitations in implicit unnesting"
    sql: |-
      -- Invalid
      WITH Coordinates AS (SELECT ARRAY<STRUCT<x INT64, y ARRAY<INT64>>>[(1, [2,3]), (4, [5,6])] AS position)
      SELECT results FROM Coordinates, Coordinates.position.y[SAFE_OFFSET(1)] AS results
    expected:
      error:
        contains: ""
  - desc: "query-syntax#21 L1307 PIVOT` operator"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'Kale' as product, 51 as sales, 'Q1' as quarter, 2020 as year UNION ALL
          SELECT 'Kale', 23, 'Q2', 2020 UNION ALL
          SELECT 'Kale', 45, 'Q3', 2020 UNION ALL
          SELECT 'Kale', 3, 'Q4', 2020 UNION ALL
          SELECT 'Kale', 70, 'Q1', 2021 UNION ALL
          SELECT 'Kale', 85, 'Q2', 2021 UNION ALL
          SELECT 'Apple', 77, 'Q1', 2020 UNION ALL
          SELECT 'Apple', 0, 'Q2', 2020 UNION ALL
          SELECT 'Apple', 1, 'Q1', 2021
    sql: |-
      SELECT * FROM
        Produce
        PIVOT(SUM(sales) FOR quarter IN ('Q1', 'Q2', 'Q3', 'Q4'))
    expected:
      rows:
        - ["Apple",2020,77,0,null,null]
        - ["Apple",2021,1,null,null,null]
        - ["Kale",2020,51,23,45,3]
        - ["Kale",2021,70,85,null,null]
      unordered: true
  - desc: "query-syntax#22 L1324 PIVOT` operator"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'Kale' as product, 51 as sales, 'Q1' as quarter, 2020 as year UNION ALL
          SELECT 'Kale', 23, 'Q2', 2020 UNION ALL
          SELECT 'Kale', 45, 'Q3', 2020 UNION ALL
          SELECT 'Kale', 3, 'Q4', 2020 UNION ALL
          SELECT 'Kale', 70, 'Q1', 2021 UNION ALL
          SELECT 'Kale', 85, 'Q2', 2021 UNION ALL
          SELECT 'Apple', 77, 'Q1', 2020 UNION ALL
          SELECT 'Apple', 0, 'Q2', 2020 UNION ALL
          SELECT 'Apple', 1, 'Q1', 2021
    sql: |-
      SELECT * FROM
        (SELECT product, sales, quarter FROM Produce)
        PIVOT(SUM(sales) FOR quarter IN ('Q1', 'Q2', 'Q3', 'Q4'))
    expected:
      rows:
        - ["Apple",78,0,null,null]
        - ["Kale",121,108,45,3]
      unordered: true
  - desc: "query-syntax#23 L1339 PIVOT` operator"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'Kale' as product, 51 as sales, 'Q1' as quarter, 2020 as year UNION ALL
          SELECT 'Kale', 23, 'Q2', 2020 UNION ALL
          SELECT 'Kale', 45, 'Q3', 2020 UNION ALL
          SELECT 'Kale', 3, 'Q4', 2020 UNION ALL
          SELECT 'Kale', 70, 'Q1', 2021 UNION ALL
          SELECT 'Kale', 85, 'Q2', 2021 UNION ALL
          SELECT 'Apple', 77, 'Q1', 2020 UNION ALL
          SELECT 'Apple', 0, 'Q2', 2020 UNION ALL
          SELECT 'Apple', 1, 'Q1', 2021
    sql: |-
      SELECT * FROM
        (SELECT product, sales, quarter FROM Produce)
        PIVOT(SUM(sales) FOR quarter IN ('Q1', 'Q2', 'Q3'))
    expected:
      rows:
        - ["Apple",78,0,null]
        - ["Kale",121,108,45]
      unordered: true
  - desc: "query-syntax#24 L1352 PIVOT` operator"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'Kale' as product, 51 as sales, 'Q1' as quarter, 2020 as year UNION ALL
          SELECT 'Kale', 23, 'Q2', 2020 UNION ALL
          SELECT 'Kale', 45, 'Q3', 2020 UNION ALL
          SELECT 'Kale', 3, 'Q4', 2020 UNION ALL
          SELECT 'Kale', 70, 'Q1', 2021 UNION ALL
          SELECT 'Kale', 85, 'Q2', 2021 UNION ALL
          SELECT 'Apple', 77, 'Q1', 2020 UNION ALL
          SELECT 'Apple', 0, 'Q2', 2020 UNION ALL
          SELECT 'Apple', 1, 'Q1', 2021
    sql: |-
      SELECT * FROM
        (SELECT sales, quarter FROM Produce)
        PIVOT(SUM(sales) FOR quarter IN ('Q1', 'Q2', 'Q3'))
    expected:
      rows:
        - [199,108,45]
  - desc: "query-syntax#25 L1368 PIVOT` operator"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'Kale' as product, 51 as sales, 'Q1' as quarter, 2020 as year UNION ALL
          SELECT 'Kale', 23, 'Q2', 2020 UNION ALL
          SELECT 'Kale', 45, 'Q3', 2020 UNION ALL
          SELECT 'Kale', 3, 'Q4', 2020 UNION ALL
          SELECT 'Kale', 70, 'Q1', 2021 UNION ALL
          SELECT 'Kale', 85, 'Q2', 2021 UNION ALL
          SELECT 'Apple', 77, 'Q1', 2020 UNION ALL
          SELECT 'Apple', 0, 'Q2', 2020 UNION ALL
          SELECT 'Apple', 1, 'Q1', 2021
    sql: |-
      SELECT * FROM
        (SELECT product, sales, quarter FROM Produce)
        PIVOT(SUM(sales) AS total_sales, COUNT(*) AS num_records FOR quarter IN ('Q1', 'Q2'))
    expected:
      rows:
        - ["Kale",121,2,108,2]
        - ["Apple",78,2,0,1]
      unordered: true
  - desc: "query-syntax#26 L1387 PIVOT` operator"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'Kale' as product, 51 as sales, 'Q1' as quarter, 2020 as year UNION ALL
          SELECT 'Kale', 23, 'Q2', 2020 UNION ALL
          SELECT 'Kale', 45, 'Q3', 2020 UNION ALL
          SELECT 'Kale', 3, 'Q4', 2020 UNION ALL
          SELECT 'Kale', 70, 'Q1', 2021 UNION ALL
          SELECT 'Kale', 85, 'Q2', 2021 UNION ALL
          SELECT 'Apple', 77, 'Q1', 2020 UNION ALL
          SELECT 'Apple', 0, 'Q2', 2020 UNION ALL
          SELECT 'Apple', 1, 'Q1', 2021
    sql: |-
      SELECT
        pivoted_sales.product,
        pivoted_sales.sales_first_quarter,
        pivoted_sales.sales_second_quarter
      FROM
        (SELECT product, sales, quarter FROM Produce)
        PIVOT(SUM(sales) AS sales FOR quarter IN ('Q1' AS first_quarter, 'Q2' AS second_quarter)) AS pivoted_sales
    expected:
      rows:
        - ["Apple",78,0]
        - ["Kale",121,108]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#27 L1590 UNPIVOT` operator"
    sql: |-
      WITH Produce AS (
        SELECT 'Kale' as product, 51 as Q1, 23 as Q2, 45 as Q3, 3 as Q4 UNION ALL
        SELECT 'Apple', 77, 0, 25, 2)
      SELECT * FROM Produce
    expected:
      rows:
        - ["Kale",51,23,45,3]
        - ["Apple",77,0,25,2]
      unordered: true
  - desc: "query-syntax#28 L1609 UNPIVOT` operator"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'Kale' as product, 51 as Q1, 23 as Q2, 45 as Q3, 3 as Q4 UNION ALL
          SELECT 'Apple', 77, 0, 25, 2
    sql: |-
      SELECT * FROM Produce
      UNPIVOT(sales FOR quarter IN (Q1, Q2, Q3, Q4))
    expected:
      rows:
        - ["Kale",51,"Q1"]
        - ["Kale",23,"Q2"]
        - ["Kale",45,"Q3"]
        - ["Kale",3,"Q4"]
        - ["Apple",77,"Q1"]
        - ["Apple",0,"Q2"]
        - ["Apple",25,"Q3"]
        - ["Apple",2,"Q4"]
      unordered: true
  - desc: "query-syntax#29 L1630 UNPIVOT` operator"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'Kale' as product, 51 as Q1, 23 as Q2, 45 as Q3, 3 as Q4 UNION ALL
          SELECT 'Apple', 77, 0, 25, 2
    sql: |-
      SELECT * FROM Produce
      UNPIVOT(
        (first_half_sales, second_half_sales)
        FOR semesters
        IN ((Q1, Q2) AS 'semester_1', (Q3, Q4) AS 'semester_2'))
    expected:
      rows:
        - ["Kale",51,23,"semester_1"]
        - ["Kale",45,3,"semester_2"]
        - ["Apple",77,0,"semester_1"]
        - ["Apple",25,2,"semester_2"]
      unordered: true
  - desc: "query-syntax#32 L1978 [INNER] JOIN"
    setup:
      - |-
        CREATE TABLE Roster AS SELECT 'Adams' as LastName, 50 as SchoolID UNION ALL
          SELECT 'Buchanan', 52 UNION ALL
          SELECT 'Coolidge', 52 UNION ALL
          SELECT 'Davis', 51 UNION ALL
          SELECT 'Eisenhower', 77
      - |-
        CREATE TABLE TeamMascot AS SELECT 50 as SchoolID, 'Jaguars' as Mascot UNION ALL
          SELECT 51, 'Knights' UNION ALL
          SELECT 52, 'Lakers' UNION ALL
          SELECT 53, 'Mustangs'
    sql: |-
      SELECT Roster.LastName, TeamMascot.Mascot
      FROM Roster JOIN TeamMascot ON Roster.SchoolID = TeamMascot.SchoolID
    expected:
      rows:
        - ["Adams","Jaguars"]
        - ["Buchanan","Lakers"]
        - ["Coolidge","Lakers"]
        - ["Davis","Knights"]
      unordered: true
  - desc: "query-syntax#39 L2163 FULL [OUTER] JOIN"
    setup:
      - |-
        CREATE TABLE Roster AS SELECT 'Adams' as LastName, 50 as SchoolID UNION ALL
          SELECT 'Buchanan', 52 UNION ALL
          SELECT 'Coolidge', 52 UNION ALL
          SELECT 'Davis', 51 UNION ALL
          SELECT 'Eisenhower', 77
      - |-
        CREATE TABLE TeamMascot AS SELECT 50 as SchoolID, 'Jaguars' as Mascot UNION ALL
          SELECT 51, 'Knights' UNION ALL
          SELECT 52, 'Lakers' UNION ALL
          SELECT 53, 'Mustangs'
    sql: |-
      SELECT Roster.LastName, TeamMascot.Mascot
      FROM Roster FULL JOIN TeamMascot ON Roster.SchoolID = TeamMascot.SchoolID
    expected:
      rows:
        - ["Adams","Jaguars"]
        - ["Buchanan","Lakers"]
        - ["Coolidge","Lakers"]
        - ["Davis","Knights"]
        - ["Eisenhower",null]
        - [null,"Mustangs"]
      unordered: true
  - desc: "query-syntax#42 L2234 LEFT [OUTER] JOIN"
    setup:
      - |-
        CREATE TABLE Roster AS SELECT 'Adams' as LastName, 50 as SchoolID UNION ALL
          SELECT 'Buchanan', 52 UNION ALL
          SELECT 'Coolidge', 52 UNION ALL
          SELECT 'Davis', 51 UNION ALL
          SELECT 'Eisenhower', 77
      - |-
        CREATE TABLE TeamMascot AS SELECT 50 as SchoolID, 'Jaguars' as Mascot UNION ALL
          SELECT 51, 'Knights' UNION ALL
          SELECT 52, 'Lakers' UNION ALL
          SELECT 53, 'Mustangs'
    sql: |-
      SELECT Roster.LastName, TeamMascot.Mascot
      FROM Roster LEFT JOIN TeamMascot ON Roster.SchoolID = TeamMascot.SchoolID
    expected:
      rows:
        - ["Adams","Jaguars"]
        - ["Buchanan","Lakers"]
        - ["Coolidge","Lakers"]
        - ["Davis","Knights"]
        - ["Eisenhower",null]
      unordered: true
  - desc: "query-syntax#45 L2304 RIGHT [OUTER] JOIN"
    setup:
      - |-
        CREATE TABLE Roster AS SELECT 'Adams' as LastName, 50 as SchoolID UNION ALL
          SELECT 'Buchanan', 52 UNION ALL
          SELECT 'Coolidge', 52 UNION ALL
          SELECT 'Davis', 51 UNION ALL
          SELECT 'Eisenhower', 77
      - |-
        CREATE TABLE TeamMascot AS SELECT 50 as SchoolID, 'Jaguars' as Mascot UNION ALL
          SELECT 51, 'Knights' UNION ALL
          SELECT 52, 'Lakers' UNION ALL
          SELECT 53, 'Mustangs'
    sql: |-
      SELECT Roster.LastName, TeamMascot.Mascot
      FROM Roster RIGHT JOIN TeamMascot ON Roster.SchoolID = TeamMascot.SchoolID
    expected:
      rows:
        - ["Adams","Jaguars"]
        - ["Buchanan","Lakers"]
        - ["Coolidge","Lakers"]
        - ["Davis","Knights"]
        - [null,"Mustangs"]
      unordered: true
  - desc: "query-syntax#46 L2374 LATERAL` join"
    skip: "not_in_bigquery: LATERAL join; BigQuery raises \"LATERAL join is not supported\"."
    setup:
      - |-
        CREATE TABLE Roster AS SELECT 'Adams' as LastName, 50 as SchoolID UNION ALL
          SELECT 'Buchanan', 52 UNION ALL
          SELECT 'Coolidge', 52 UNION ALL
          SELECT 'Davis', 51 UNION ALL
          SELECT 'Eisenhower', 77
      - |-
        CREATE TABLE PlayerStats AS SELECT 'Adams' as LastName, 51 as OpponentID, 3 as PointsScored UNION ALL
          SELECT 'Buchanan', 77, 0 UNION ALL
          SELECT 'Coolidge', 77, 1 UNION ALL
          SELECT 'Adams', 52, 4 UNION ALL
          SELECT 'Buchanan', 50, 13
    sql: |-
      SELECT R.SchoolID, OP.LastName AS TopOpPlayer, OP.PointsScored
      FROM Roster AS R,
        LATERAL(
          SELECT PS.LastName, PS.PointsScored
          FROM PlayerStats AS PS
          WHERE PS.OpponentID = R.SchoolID
          ORDER BY PointsScored DESC
          LIMIT 1
        ) AS OP
      ORDER BY R.SchoolID
    expected:
      rows:
        - ["50","Buchanan","13"]
        - ["51","Adams","3"]
        - ["52","Adams","4"]
        - ["57","Coolidge","1"]
  - desc: "query-syntax#47 L2402 LATERAL` join"
    skip: "not_in_bigquery: LATERAL join; BigQuery raises \"LATERAL join is not supported\"."
    setup:
      - |-
        CREATE TABLE Roster AS SELECT 'Adams' as LastName, 50 as SchoolID UNION ALL
          SELECT 'Buchanan', 52 UNION ALL
          SELECT 'Coolidge', 52 UNION ALL
          SELECT 'Davis', 51 UNION ALL
          SELECT 'Eisenhower', 77
      - |-
        CREATE TABLE TeamMascot AS SELECT 50 as SchoolID, 'Jaguars' as Mascot UNION ALL
          SELECT 51, 'Knights' UNION ALL
          SELECT 52, 'Lakers' UNION ALL
          SELECT 53, 'Mustangs'
    sql: |-
      SELECT R.LastName, R.SchoolID, M.Mascot
      FROM Roster AS R
      LEFT JOIN
        LATERAL(
          SELECT Mascot
          FROM TeamMascot m
          WHERE m.SchoolID = R.SchoolID
        ) AS M
      ORDER BY R.LastName
    expected:
      rows:
        - ["Adams","50","Jaguars"]
        - ["Buchanan","52","Lakers"]
        - ["Coolidge","52","Lakers"]
        - ["Davis","51","Knights"]
        - ["Eisenhower","77",null]
  - desc: "query-syntax#52 L2796 USING` clause"
    setup:
      - |-
        CREATE TABLE Roster AS SELECT 'Adams' as LastName, 50 as SchoolID UNION ALL
          SELECT 'Buchanan', 52 UNION ALL
          SELECT 'Coolidge', 52 UNION ALL
          SELECT 'Davis', 51 UNION ALL
          SELECT 'Eisenhower', 77
      - |-
        CREATE TABLE TeamMascot AS SELECT 50 as SchoolID, 'Jaguars' as Mascot UNION ALL
          SELECT 51, 'Knights' UNION ALL
          SELECT 52, 'Lakers' UNION ALL
          SELECT 53, 'Mustangs'
    sql: |-
      SELECT * FROM Roster INNER JOIN TeamMascot USING (SchoolID)
    expected:
      rows:
        - [50,"Adams","Jaguars"]
        - [52,"Buchanan","Lakers"]
        - [52,"Coolidge","Lakers"]
        - [51,"Davis","Knights"]
      unordered: true
  - desc: "query-syntax#53 L3188 Correlated join operation"
    setup:
      - |-
        CREATE TABLE Roster AS SELECT 'Adams' as LastName, 50 as SchoolID UNION ALL
          SELECT 'Buchanan', 52 UNION ALL
          SELECT 'Coolidge', 52 UNION ALL
          SELECT 'Davis', 51 UNION ALL
          SELECT 'Eisenhower', 77
      - |-
        CREATE TABLE PlayerStats AS SELECT 'Adams' as LastName, 51 as OpponentID, 3 as PointsScored UNION ALL
          SELECT 'Buchanan', 77, 0 UNION ALL
          SELECT 'Coolidge', 77, 1 UNION ALL
          SELECT 'Adams', 52, 4 UNION ALL
          SELECT 'Buchanan', 50, 13
    sql: |-
      SELECT *
      FROM
        Roster
      JOIN
        UNNEST(
          ARRAY(
            SELECT AS STRUCT *
            FROM PlayerStats
            WHERE PlayerStats.OpponentID = Roster.SchoolID
          )) AS PlayerMatches
        ON PlayerMatches.LastName = 'Buchanan'
    expected:
      rows:
        - ["Adams",50,"Buchanan",50,13]
        - ["Eisenhower",77,"Buchanan",77,0]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#56 L3411 Group rows by values"
    sql: |-
      WITH PlayerStats AS (
        SELECT 'Adams' as LastName, 'Noam' as FirstName, 3 as PointsScored UNION ALL
        SELECT 'Buchanan', 'Jie', 0 UNION ALL
        SELECT 'Coolidge', 'Kiran', 1 UNION ALL
        SELECT 'Adams', 'Noam', 4 UNION ALL
        SELECT 'Buchanan', 'Jie', 13)
      SELECT SUM(PointsScored) AS total_points, LastName
      FROM PlayerStats
      GROUP BY LastName
    expected:
      rows:
        - [7,"Adams"]
        - [13,"Buchanan"]
        - [1,"Coolidge"]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#57 L3435 Group rows by values"
    sql: |-
      WITH PlayerStats AS (
        SELECT 'Adams' as LastName, 'Noam' as FirstName, 3 as PointsScored UNION ALL
        SELECT 'Buchanan', 'Jie', 0 UNION ALL
        SELECT 'Coolidge', 'Kiran', 1 UNION ALL
        SELECT 'Adams', 'Noam', 4 UNION ALL
        SELECT 'Buchanan', 'Jie', 13)
      SELECT SUM(PointsScored) AS total_points, LastName AS last_name
      FROM PlayerStats
      GROUP BY last_name
    expected:
      rows:
        - [7,"Adams"]
        - [13,"Buchanan"]
        - [1,"Coolidge"]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#58 L3458 Group rows by values"
    sql: |-
      WITH PlayerStats AS (
        SELECT ['Coolidge', 'Adams'] as Name, 3 as PointsScored UNION ALL
        SELECT ['Adams', 'Buchanan'], 0 UNION ALL
        SELECT ['Coolidge', 'Adams'], 1 UNION ALL
        SELECT ['Kiran', 'Noam'], 1)
      SELECT SUM(PointsScored) AS total_points, name
      FROM PlayerStats
      GROUP BY Name
    expected:
      rows:
        - [4,["Coolidge","Adams"]]
        - [0,["Adams","Buchanan"]]
        - [1,["Kiran","Noam"]]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#60 L3537 Group rows by column ordinals"
    sql: |-
      WITH PlayerStats AS (
        SELECT 'Adams' as LastName, 'Noam' as FirstName, 3 as PointsScored UNION ALL
        SELECT 'Buchanan', 'Jie', 0 UNION ALL
        SELECT 'Coolidge', 'Kiran', 1 UNION ALL
        SELECT 'Adams', 'Noam', 4 UNION ALL
        SELECT 'Buchanan', 'Jie', 13)
      SELECT SUM(PointsScored) AS total_points, LastName, FirstName
      FROM PlayerStats
      GROUP BY LastName, FirstName
    expected:
      rows:
        - [7,"Adams","Noam"]
        - [13,"Buchanan","Jie"]
        - [1,"Coolidge","Kiran"]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#61 L3557 Group rows by column ordinals"
    sql: |-
      WITH PlayerStats AS (
        SELECT 'Adams' as LastName, 'Noam' as FirstName, 3 as PointsScored UNION ALL
        SELECT 'Buchanan', 'Jie', 0 UNION ALL
        SELECT 'Coolidge', 'Kiran', 1 UNION ALL
        SELECT 'Adams', 'Noam', 4 UNION ALL
        SELECT 'Buchanan', 'Jie', 13)
      SELECT SUM(PointsScored) AS total_points, LastName, FirstName
      FROM PlayerStats
      GROUP BY 2, 3
    expected:
      rows:
        - [7,"Adams","Noam"]
        - [13,"Buchanan","Jie"]
        - [1,"Coolidge","Kiran"]
      unordered: true
  - desc: "query-syntax#66 L3823 Group rows by `GROUPING SETS"
    skip: "nondeterministic: ORDER BY product_name ties the two NULL product_name rows (shirt, pants); their order is unspecified."
    sql: |-
      -- GROUP BY with GROUPING SETS
      WITH
        Products AS (
          SELECT 'shirt' AS product_type, 't-shirt' AS product_name, 3 AS product_count UNION ALL
          SELECT 'shirt', 't-shirt', 8 UNION ALL
          SELECT 'shirt', 'polo', 25 UNION ALL
          SELECT 'pants', 'jeans', 6
        )
      SELECT product_type, product_name, SUM(product_count) AS product_sum
      FROM Products
      GROUP BY GROUPING SETS (product_type, product_name)
      ORDER BY product_name
    expected:
      rows:
        - ["shirt",null,36]
        - ["pants",null,6]
        - [null,"jeans",6]
        - [null,"polo",25]
        - [null,"t-shirt",11]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#67 L3871 Group rows by `GROUPING SETS"
    sql: |-
      -- GROUP BY with GROUPING SETS and a groupable item set
      WITH
        Products AS (
          SELECT 'shirt' AS product_type, 't-shirt' AS product_name, 3 AS product_count UNION ALL
          SELECT 'shirt', 't-shirt', 8 UNION ALL
          SELECT 'shirt', 'polo', 25 UNION ALL
          SELECT 'pants', 'jeans', 6
        )
      SELECT product_type, product_name, SUM(product_count) AS product_sum
      FROM Products
      GROUP BY GROUPING SETS (
        product_type,
        (product_type, product_name))
      ORDER BY product_type, product_name
    expected:
      rows:
        - ["pants",null,6]
        - ["pants","jeans",6]
        - ["shirt",null,36]
        - ["shirt","polo",25]
        - ["shirt","t-shirt",11]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#68 L3921 Group rows by `GROUPING SETS"
    sql: |-
      -- GROUP BY with GROUPING SETS and ROLLUP
      WITH
        Products AS (
          SELECT 'shirt' AS product_type, 't-shirt' AS product_name, 3 AS product_count UNION ALL
          SELECT 'shirt', 't-shirt', 8 UNION ALL
          SELECT 'shirt', 'polo', 25 UNION ALL
          SELECT 'pants', 'jeans', 6
        )
      SELECT product_type, product_name, SUM(product_count) AS product_sum
      FROM Products
      GROUP BY GROUPING SETS (
        product_type,
        ROLLUP (product_type, product_name))
      ORDER BY product_type, product_name
    expected:
      rows:
        - [null,null,42]
        - ["pants",null,6]
        - ["pants",null,6]
        - ["pants","jeans",6]
        - ["shirt",null,36]
        - ["shirt",null,36]
        - ["shirt","polo",25]
        - ["shirt","t-shirt",11]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#69 L3974 Group rows by `GROUPING SETS"
    sql: |-
      -- GROUP BY with GROUPING SETS and CUBE
      WITH
        Products AS (
          SELECT 'shirt' AS product_type, 't-shirt' AS product_name, 3 AS product_count UNION ALL
          SELECT 'shirt', 't-shirt', 8 UNION ALL
          SELECT 'shirt', 'polo', 25 UNION ALL
          SELECT 'pants', 'jeans', 6
        )
      SELECT product_type, product_name, SUM(product_count) AS product_sum
      FROM Products
      GROUP BY GROUPING SETS (
        product_type,
        CUBE (product_type, product_name))
      ORDER BY product_type, product_name
    expected:
      rows:
        - [null,null,42]
        - [null,"jeans",6]
        - [null,"polo",25]
        - [null,"t-shirt",11]
        - ["pants",null,6]
        - ["pants",null,6]
        - ["pants","jeans",6]
        - ["shirt",null,36]
        - ["shirt",null,36]
        - ["shirt","polo",25]
        - ["shirt","t-shirt",11]
  # Follows BigQuery, not the docs: query-syntax.md, Group rows by ROLLUP, lists (shirt, t-shirt) before (shirt, polo) under ORDER BY product_type, product_name. BigQuery sorts polo before t-shirt.
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#70 L4099 Group rows by `ROLLUP"
    sql: |-
      -- GROUP BY with ROLLUP
      WITH
        Products AS (
          SELECT 'shirt' AS product_type, 't-shirt' AS product_name, 3 AS product_count UNION ALL
          SELECT 'shirt', 't-shirt', 8 UNION ALL
          SELECT 'shirt', 'polo', 25 UNION ALL
          SELECT 'pants', 'jeans', 6
        )
      SELECT product_type, product_name, SUM(product_count) AS product_sum
      FROM Products
      GROUP BY ROLLUP (product_type, product_name)
      ORDER BY product_type, product_name
    expected:
      rows:
        - [null,null,42]
        - ["pants",null,6]
        - ["pants","jeans",6]
        - ["shirt",null,36]
        - ["shirt","polo",25]
        - ["shirt","t-shirt",11]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#71 L4150 Group rows by `ROLLUP"
    sql: |-
      WITH
        Products AS (
          SELECT 'shirt' AS product_type, 't-shirt' AS product_name, 3 AS product_count UNION ALL
          SELECT 'shirt', 't-shirt', 8 UNION ALL
          SELECT 'shirt', 'polo', 25 UNION ALL
          SELECT 'pants', 'jeans', 6
        )
      SELECT product_type, product_name, SUM(product_count) AS product_sum
      FROM Products
      GROUP BY ROLLUP (product_type, (product_type, product_name))
      ORDER BY product_type, product_name
    expected:
      rows:
        - [null,null,42]
        - ["pants",null,6]
        - ["pants","jeans",6]
        - ["shirt",null,36]
        - ["shirt","polo",25]
        - ["shirt","t-shirt",11]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#72 L4254 Group rows by `CUBE"
    sql: |-
      -- GROUP BY with CUBE
      WITH
        Products AS (
          SELECT 'shirt' AS product_type, 't-shirt' AS product_name, 3 AS product_count UNION ALL
          SELECT 'shirt', 't-shirt', 8 UNION ALL
          SELECT 'shirt', 'polo', 25 UNION ALL
          SELECT 'pants', 'jeans', 6
        )
      SELECT product_type, product_name, SUM(product_count) AS product_sum
      FROM Products
      GROUP BY CUBE (product_type, product_name)
      ORDER BY product_type, product_name
    expected:
      rows:
        - [null,null,42]
        - [null,"jeans",6]
        - [null,"polo",25]
        - [null,"t-shirt",11]
        - ["pants",null,6]
        - ["pants","jeans",6]
        - ["shirt",null,36]
        - ["shirt","polo",25]
        - ["shirt","t-shirt",11]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#73 L4287 Group rows by `CUBE"
    sql: |-
      WITH
        Products AS (
          SELECT 'shirt' AS product_type, 't-shirt' AS product_name, 3 AS product_count UNION ALL
          SELECT 'shirt', 't-shirt', 8 UNION ALL
          SELECT 'shirt', 'polo', 25 UNION ALL
          SELECT 'pants', 'jeans', 6
        )
      SELECT product_type, product_name, SUM(product_count) AS product_sum
      FROM Products
      GROUP BY CUBE (product_type, (product_type, product_name))
      ORDER BY product_type, product_name
    expected:
      rows:
        - [null,null,42]
        - ["pants",null,6]
        - ["pants","jeans",6]
        - ["pants","jeans",6]
        - ["shirt",null,36]
        - ["shirt","polo",25]
        - ["shirt","polo",25]
        - ["shirt","t-shirt",11]
        - ["shirt","t-shirt",11]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#74 L4463 ORDER BY` clause"
    sql: |-
      SELECT x, y
      FROM (SELECT 1 AS x, true AS y UNION ALL
            SELECT 9, true UNION ALL
            SELECT NULL, false)
      ORDER BY x
    expected:
      rows:
        - [null,false]
        - [1,true]
        - [9,true]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#75 L4481 ORDER BY` clause"
    sql: |-
      SELECT x, y
      FROM (SELECT 1 AS x, true AS y UNION ALL
            SELECT 9, true UNION ALL
            SELECT NULL, false)
      ORDER BY x NULLS LAST
    expected:
      rows:
        - [1,true]
        - [9,true]
        - [null,false]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#76 L4499 ORDER BY` clause"
    sql: |-
      SELECT x, y
      FROM (SELECT 1 AS x, true AS y UNION ALL
            SELECT 9, true UNION ALL
            SELECT NULL, false)
      ORDER BY x DESC
    expected:
      rows:
        - [9,true]
        - [1,true]
        - [null,false]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#77 L4517 ORDER BY` clause"
    sql: |-
      SELECT x, y
      FROM (SELECT 1 AS x, true AS y UNION ALL
            SELECT 9, true UNION ALL
            SELECT NULL, false)
      ORDER BY x DESC NULLS FIRST
    expected:
      rows:
        - [null,false]
        - [9,true]
        - [1,true]
  - desc: "query-syntax#78 L4674 QUALIFY` clause"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'kale' as item, 23 as purchases, 'vegetable' as category
          UNION ALL SELECT 'banana', 2, 'fruit'
          UNION ALL SELECT 'cabbage', 9, 'vegetable'
          UNION ALL SELECT 'apple', 8, 'fruit'
          UNION ALL SELECT 'leek', 2, 'vegetable'
          UNION ALL SELECT 'lettuce', 10, 'vegetable'
    sql: |-
      SELECT
        item,
        RANK() OVER (PARTITION BY category ORDER BY purchases DESC) as rank
      FROM Produce
      WHERE Produce.category = 'vegetable'
      QUALIFY rank <= 3
    expected:
      rows:
        - ["kale",1]
        - ["lettuce",2]
        - ["cabbage",3]
      unordered: true
  - desc: "query-syntax#79 L4695 QUALIFY` clause"
    setup:
      - |-
        CREATE TABLE Produce AS SELECT 'kale' as item, 23 as purchases, 'vegetable' as category
          UNION ALL SELECT 'banana', 2, 'fruit'
          UNION ALL SELECT 'cabbage', 9, 'vegetable'
          UNION ALL SELECT 'apple', 8, 'fruit'
          UNION ALL SELECT 'leek', 2, 'vegetable'
          UNION ALL SELECT 'lettuce', 10, 'vegetable'
    sql: |-
      SELECT item
      FROM Produce
      WHERE Produce.category = 'vegetable'
      QUALIFY RANK() OVER (PARTITION BY category ORDER BY purchases DESC) <= 3
    expected:
      rows:
        - ["kale"]
        - ["lettuce"]
        - ["cabbage"]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#80 L4831 Set operators"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit
      UNION ALL BY NAME
      SELECT 20 AS two_digit, 2 AS one_digit
    expected:
      rows:
        - [1,10]
        - [2,20]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#81 L5082 UNION"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3]) AS number
      UNION ALL
      SELECT 1
    expected:
      rows:
        - [1]
        - [2]
        - [3]
        - [1]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#82 L5097 UNION"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3]) AS number
      UNION DISTINCT
      SELECT 1
    expected:
      rows:
        - [1]
        - [2]
        - [3]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#83 L5113 UNION"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3]) AS number
      UNION DISTINCT
      SELECT 1
      UNION DISTINCT
      SELECT 2
    expected:
      rows:
        - [1]
        - [2]
        - [3]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#84 L5134 UNION"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit
      UNION ALL
      SELECT 20 AS two_digit, 2 AS one_digit
    expected:
      rows:
        - [1,10]
        - [20,2]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#86 L5170 UNION"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit
      UNION ALL STRICT CORRESPONDING
      SELECT 20 AS two_digit, 2 AS one_digit
    expected:
      rows:
        - [1,10]
        - [2,20]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#87 L5191 UNION"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit, 100 AS three_digit
      INNER UNION ALL BY NAME
      SELECT 20 AS two_digit, 2 AS one_digit, 1000 AS four_digit
    expected:
      rows:
        - [1,10]
        - [2,20]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#88 L5208 UNION"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit, 100 AS three_digit
      FULL OUTER UNION ALL BY NAME
      SELECT 20 AS two_digit, 2 AS one_digit, 1000 AS four_digit
    expected:
      rows:
        - [1,10,100,null]
        - [2,20,null,1000]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#89 L5225 UNION"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit, 100 AS three_digit
      LEFT OUTER UNION ALL BY NAME
      SELECT 20 AS two_digit, 2 AS one_digit, 1000 AS four_digit
    expected:
      rows:
        - [1,10,100]
        - [2,20,null]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#90 L5241 UNION"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit, 100 AS three_digit
      FULL OUTER UNION ALL BY NAME ON (three_digit, two_digit)
      SELECT 20 AS two_digit, 2 AS one_digit, 1000 AS four_digit
    expected:
      rows:
        - [100,10]
        - [null,20]
      unordered: true
  - desc: "query-syntax#91 L5262 INTERSECT"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      INTERSECT ALL
      SELECT * FROM UNNEST(ARRAY<INT64>[2, 3, 3, 5]) AS number
    expected:
      rows:
        - [2]
        - [3]
        - [3]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#92 L5276 INTERSECT"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      INTERSECT DISTINCT
      SELECT * FROM UNNEST(ARRAY<INT64>[2, 3, 3, 5]) AS number
    expected:
      rows:
        - [2]
        - [3]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#93 L5291 INTERSECT"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      INTERSECT DISTINCT
      SELECT * FROM UNNEST(ARRAY<INT64>[2, 3, 3, 5]) AS number
      INTERSECT DISTINCT
      SELECT * FROM UNNEST(ARRAY<INT64>[3, 3, 4, 5]) AS number
    expected:
      rows:
        - [3]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#95 L5333 INTERSECT"
    sql: |-
      WITH
        NumbersTable AS (
          SELECT 1 AS one_digit, 10 AS two_digit
          UNION ALL
          SELECT 2, 20
          UNION ALL
          SELECT 3, 30
        )
      SELECT one_digit, two_digit FROM NumbersTable
      INTERSECT DISTINCT BY NAME
      SELECT 10 AS two_digit, 1 AS one_digit
    expected:
      rows:
        - [1,10]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#96 L5357 INTERSECT"
    sql: |-
      WITH
        NumbersTable AS (
          SELECT 1 AS one_digit, 10 AS two_digit
          UNION ALL
          SELECT 2, 20
          UNION ALL
          SELECT 3, 30
        )
      SELECT one_digit, two_digit FROM NumbersTable
      INTERSECT DISTINCT STRICT CORRESPONDING
      SELECT 10 AS two_digit, 1 AS one_digit
    expected:
      rows:
        - [1,10]
  - desc: "query-syntax#97 L5388 EXCEPT"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      EXCEPT ALL
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2]) AS number
    expected:
      rows:
        - [3]
        - [3]
        - [4]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#98 L5402 EXCEPT"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      EXCEPT DISTINCT
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2]) AS number
    expected:
      rows:
        - [3]
        - [4]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#99 L5417 EXCEPT"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      EXCEPT DISTINCT
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2]) AS number
      EXCEPT DISTINCT
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 4]) AS number
    expected:
      rows:
        - [3]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#100 L5438 EXCEPT"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      EXCEPT DISTINCT
      (
        SELECT * FROM UNNEST(ARRAY<INT64>[1, 2]) AS number
        EXCEPT DISTINCT
        SELECT * FROM UNNEST(ARRAY<INT64>[1, 4]) AS number
      )
    expected:
      rows:
        - [1]
        - [3]
        - [4]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#101 L5462 EXCEPT"
    sql: |-
      WITH
        NumbersTable AS (
          SELECT 1 AS one_digit, 10 AS two_digit
          UNION ALL
          SELECT 2, 20
          UNION ALL
          SELECT 3, 30
        )
      SELECT one_digit, two_digit FROM NumbersTable
      EXCEPT DISTINCT
      SELECT 10 AS two_digit, 1 AS one_digit
    expected:
      rows:
        - [1,10]
        - [2,20]
        - [3,30]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#102 L5489 EXCEPT"
    sql: |-
      WITH
        NumbersTable AS (
          SELECT 1 AS one_digit, 10 AS two_digit
          UNION ALL
          SELECT 2, 20
          UNION ALL
          SELECT 3, 30
        )
      SELECT one_digit, two_digit FROM NumbersTable
      EXCEPT DISTINCT BY NAME
      SELECT 10 AS two_digit, 1 AS one_digit
    expected:
      rows:
        - [2,20]
        - [3,30]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#103 L5514 EXCEPT"
    sql: |-
      WITH
        NumbersTable AS (
          SELECT 1 AS one_digit, 10 AS two_digit
          UNION ALL
          SELECT 2, 20
          UNION ALL
          SELECT 3, 30
        )
      SELECT one_digit, two_digit FROM NumbersTable
      EXCEPT DISTINCT STRICT CORRESPONDING
      SELECT 10 AS two_digit, 1 AS one_digit
    expected:
      rows:
        - [2,20]
        - [3,30]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#104 L5580 LIMIT` and `OFFSET` clause"
    sql: |-
      SELECT *
      FROM UNNEST(ARRAY<STRING>['a', 'b', 'c', 'd', 'e']) AS letter
      ORDER BY letter ASC LIMIT 2
    expected:
      rows:
        - ["a"]
        - ["b"]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#105 L5593 LIMIT` and `OFFSET` clause"
    sql: |-
      SELECT *
      FROM UNNEST(ARRAY<STRING>['a', 'b', 'c', 'd', 'e']) AS letter
      ORDER BY letter ASC LIMIT 3 OFFSET 1
    expected:
      rows:
        - ["b"]
        - ["c"]
        - ["d"]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#106 L5748 Recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS ( (SELECT 1 AS n) UNION ALL (SELECT n + 1 AS n FROM T1 WHERE n < 3) )
      SELECT n FROM T1
    expected:
      rows:
        - [2]
        - [1]
        - [3]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#107 L5786 Examples of allowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS (
          (SELECT 1 AS n) UNION ALL
          (SELECT n + 2 FROM T1 WHERE n < 4))
      SELECT * FROM T1 ORDER BY n
    expected:
      rows:
        - [1]
        - [3]
        - [5]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#108 L5806 Examples of allowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T0 AS (SELECT 1 AS n),
        T1 AS ((SELECT * FROM T0) UNION ALL (SELECT n + 1 FROM T1 WHERE n < 4)),
        T2 AS ((SELECT 1 AS n) UNION ALL (SELECT n + 1 FROM T2 WHERE n < 4)),
        T3 AS (SELECT * FROM T1 INNER JOIN T2 USING (n))
      SELECT * FROM T3 ORDER BY n
    expected:
      rows:
        - [1]
        - [2]
        - [3]
        - [4]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#109 L5827 Examples of allowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T0 AS (SELECT * FROM UNNEST ([60, 20, 30])),
        T1 AS ((SELECT 1 AS n) UNION ALL (SELECT n + (SELECT COUNT(*) FROM T0) FROM T1 WHERE n < 4))
      SELECT * FROM T1 ORDER BY n
    expected:
      rows:
        - [1]
        - [4]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#110 L5843 Examples of allowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T0 AS (SELECT 1 AS n),
        T1 AS ((SELECT 1 AS n) UNION ALL (SELECT n + 1 FROM T1 INNER JOIN T0 USING (n)))
      SELECT * FROM T1 ORDER BY n
    expected:
      rows:
        - [1]
        - [2]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#111 L5859 Examples of allowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T0 AS (SELECT 2 AS p),
        T1 AS ((SELECT 1 AS n) UNION ALL (SELECT T1.n + T0.p FROM T1 CROSS JOIN T0 WHERE T1.n < 4))
      SELECT * FROM T1 CROSS JOIN T0 ORDER BY n
    expected:
      rows:
        - [1,2]
        - [3,2]
        - [5,2]
  - desc: "query-syntax#112 L5879 Examples of allowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS ( (SELECT 0 AS n) UNION DISTINCT (SELECT MOD(n + 1, 5) FROM T1) )
      SELECT * FROM T1 ORDER BY n
    expected:
      rows:
        - [0]
        - [1]
        - [2]
        - [3]
        - [4]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#113 L5901 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS (SELECT * FROM T1)
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#114 L5912 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS ((SELECT * FROM T1) UNION ALL (SELECT 1))
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#115 L5923 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS ((SELECT 1 AS n) UNION ALL ((SELECT * FROM T1) UNION ALL (SELECT * FROM T1)))
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#116 L5934 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS ((SELECT 1 AS n) UNION ALL (SELECT (SELECT n FROM T1)))
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#117 L5945 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS (
          (SELECT 1 AS n) UNION ALL
          (SELECT * FROM MY_TVF(T1)))
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#118 L5958 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T0 AS (SELECT 1 AS n),
        T1 AS ((SELECT 1 AS n) UNION ALL (SELECT * FROM T1 FULL OUTER JOIN T0 USING (n)))
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#119 L5970 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS (
          (SELECT 1 AS n) UNION ALL
          (SELECT COUNT(*) FROM T1))
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#120 L5983 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS (
          (SELECT 1.0 AS n) UNION ALL
          SELECT 1 + AVG(n) OVER(ROWS between 2 PRECEDING and 0 FOLLOWING) FROM T1 WHERE n < 10)
      SELECT n FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#122 L6007 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS ((SELECT 1 AS n) UNION ALL (SELECT n + 1 FROM T1 ORDER BY n))
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#123 L6018 Examples of disallowed recursive CTEs"
    sql: |-
      WITH RECURSIVE
        T1 AS ((SELECT 1 AS n) UNION ALL (WITH t AS (SELECT n FROM T1) SELECT * FROM t))
      SELECT * FROM T1
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#124 L6113 Visibility of CTEs in a `WITH` clause with the `RECURSIVE` keyword"
    sql: |-
      WITH RECURSIVE
        A AS (SELECT 1 AS n UNION ALL (SELECT n + 1 FROM A WHERE n < 3))
      SELECT * FROM A
    expected:
      rows:
        - [1]
        - [2]
        - [3]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#125 L6129 Visibility of CTEs in a `WITH` clause with the `RECURSIVE` keyword"
    sql: |-
      WITH RECURSIVE
        A AS (SELECT * FROM B),
        B AS (SELECT 1 AS n)
      SELECT * FROM B
    expected:
      rows:
        - [1]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#126 L6144 Visibility of CTEs in a `WITH` clause with the `RECURSIVE` keyword"
    sql: |-
      WITH RECURSIVE
        A AS (SELECT 1 AS n),
        B AS (SELECT * FROM A)
      SELECT * FROM B
    expected:
      rows:
        - [1]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#127 L6159 Visibility of CTEs in a `WITH` clause with the `RECURSIVE` keyword"
    sql: |-
      WITH RECURSIVE
        A AS (SELECT * FROM B),
        B AS (SELECT * FROM A)
      SELECT * FROM B
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#128 L6186 Visibility of CTEs in a `WITH` clause without the `RECURSIVE` keyword"
    sql: |-
      WITH
        A AS (SELECT 1 AS n UNION ALL (SELECT n + 1 FROM A WHERE n < 3))
      SELECT * FROM A
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#129 L6197 Visibility of CTEs in a `WITH` clause without the `RECURSIVE` keyword"
    sql: |-
      WITH
        A AS (SELECT * FROM B),
        B AS (SELECT 1 AS n)
      SELECT * FROM B
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#130 L6208 Visibility of CTEs in a `WITH` clause without the `RECURSIVE` keyword"
    sql: |-
      WITH
        A AS (SELECT 1 AS n),
        B AS (SELECT * FROM A)
      SELECT * FROM B
    expected:
      rows:
        - [1]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#131 L6224 Visibility of CTEs in a `WITH` clause without the `RECURSIVE` keyword"
    sql: |-
      WITH
        A AS (SELECT * FROM B),
        B AS (SELECT * FROM A)
      SELECT * FROM B
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#132 L6291 AGGREGATION_THRESHOLD` clause"
    sql: |-
      WITH ExamTable AS (
        SELECT "Hansen" AS last_name, "P91" AS test_id, 510 AS test_score UNION ALL
        SELECT "Wang", "U25", 500 UNION ALL
        SELECT "Wang", "C83", 520 UNION ALL
        SELECT "Wang", "U25", 460 UNION ALL
        SELECT "Hansen", "C83", 420 UNION ALL
        SELECT "Hansen", "C83", 560 UNION ALL
        SELECT "Devi", "U25", 580 UNION ALL
        SELECT "Devi", "P91", 480 UNION ALL
        SELECT "Ivanov", "U25", 490 UNION ALL
        SELECT "Ivanov", "P91", 540 UNION ALL
        SELECT "Silva", "U25", 550)
      SELECT WITH AGGREGATION_THRESHOLD
        OPTIONS(threshold=3, privacy_unit_column=last_name)
        test_id,
        COUNT(DISTINCT last_name) AS student_count,
        AVG(test_score) AS avg_test_score
      FROM ExamTable
      GROUP BY test_id
    expected:
      rows:
        - ["P91",3,510]
        - ["U25",4,516]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#140 L7001 Duplicate aliases"
    sql: |-
      SELECT 1 AS a, 2 AS a
    expected:
      rows:
        - [1,2]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#141 L7047 Ambiguous aliases"
    sql: |-
      SELECT FirstName, LastName, PrimaryContact AS P
      FROM Person AS P
      GROUP BY P.FirstName
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#142 L7101 Range variables"
    sql: |-
      WITH Grid AS (SELECT 1 x, 2 y)
      SELECT Coordinate.x FROM Grid AS Coordinate
    expected:
      rows:
        - [1]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#143 L7115 Range variables"
    sql: |-
      WITH Grid AS (SELECT 1 x, 2 y)
      SELECT Coordinate.* FROM Grid AS Coordinate
    expected:
      rows:
        - [1,2]
  # Verified on BigQuery 2026-09-25.
  - desc: "query-syntax#144 L7131 Range variables"
    sql: |-
      WITH Grid AS (SELECT 1 x, 2 y)
      SELECT Coordinate FROM Grid AS Coordinate
    expected:
      rows:
        - [[1,2]]
  - desc: "query-syntax#145 L7182 Table function calls"
    skip: "docs_artifact: The docs never define the CustomerCreationTimeRange TVF or MyCustomerTable."
    sql: |-
      SELECT *
      FROM
        CustomerCreationTimeRange(
          1577836800,  -- 2020-01-01 00:00:00 UTC
          1609459199,  -- 2020-12-31 23:59:59 UTC
          TABLE MyCustomerTable)
    expected:
      rows:
        - ["Adams","50"]
        - ["Buchanan","52"]
        - ["Coolidge","52"]
        - ["Davis","51"]
        - ["Eisenhower","77"]
      unordered: true
