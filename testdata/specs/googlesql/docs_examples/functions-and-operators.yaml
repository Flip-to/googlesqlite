# Generated by TestDocsExamples from google/googlesql@d82db99 docs/functions-and-operators.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "functions-and-operators#89 L5156 KEYS.KEYSET_LENGTH"
    skip: "docs_artifact: The example reads json_keyset, which the docs never define."
    sql: |-
      SELECT KEYS.KEYSET_LENGTH(KEYS.KEYSET_FROM_JSON(json_keyset)) as key_count
    expected:
      rows:
        - ["2"]
  - desc: "functions-and-operators#223 L9950 GENERATE_DATE_ARRAY"
    setup:
      - |-
        CREATE TABLE Items AS SELECT STRUCT('blue' AS color, 'round' AS shape) AS info UNION ALL
            SELECT STRUCT('blue', 'square') UNION ALL
            SELECT STRUCT('red', 'round')
    sql: |-
      SELECT GENERATE_DATE_ARRAY(date_start, date_end, INTERVAL 1 WEEK) AS date_range
      FROM (
        SELECT DATE '2016-01-01' AS date_start, DATE '2016-01-31' AS date_end
        UNION ALL SELECT DATE "2016-04-01", DATE "2016-04-30"
        UNION ALL SELECT DATE "2016-07-01", DATE "2016-07-31"
        UNION ALL SELECT DATE "2016-10-01", DATE "2016-10-31"
      ) AS items
    expected:
      rows:
        - [["2016-01-01","2016-01-08","2016-01-15","2016-01-22","2016-01-29"]]
        - [["2016-04-01","2016-04-08","2016-04-15","2016-04-22","2016-04-29"]]
        - [["2016-07-01","2016-07-08","2016-07-15","2016-07-22","2016-07-29"]]
        - [["2016-10-01","2016-10-08","2016-10-15","2016-10-22","2016-10-29"]]
      unordered: true
  - desc: "functions-and-operators#968 L33298 MAP_CARDINALITY"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('color', 'red'), ('condition', 'good'), ('year', '2023')]) AS input_map)
      SELECT
        MAP_CARDINALITY(input_map) AS results
      FROM t
    expected:
      rows:
        - ["3"]
  - desc: "functions-and-operators#969 L33335 MAP_CONTAINS_KEY"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('color', 'red')]) AS input_map)
      SELECT
        MAP_CONTAINS_KEY(input_map, 'color') AS color_key,
        MAP_CONTAINS_KEY(input_map, 'shape') AS shape_key
      FROM t
    expected:
      rows:
        - ["true","false"]
  - desc: "functions-and-operators#970 L33376 MAP_DELETE"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), ('b', 2), ('c', 3)]) AS input_map)
      SELECT
        MAP_DELETE(
          input_map,
          'a', 'c', 'd'
        ) AS results
      FROM t
    expected:
      rows:
        - ["{\"b\": 2}"]
  - desc: "functions-and-operators#971 L33414 MAP_EMPTY"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY(CAST([] AS ARRAY<STRUCT<INT64, INT64>>)) AS input_map)
      SELECT
        MAP_EMPTY(input_map) AS empty
      FROM t
    expected:
      rows:
        - ["true"]
  - desc: "functions-and-operators#972 L33452 MAP_ENTRIES_SORTED"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('b', 1), ('a', 2), ('c', 3)]) AS input_map)
      SELECT MAP_ENTRIES_SORTED(input_map) AS results
      FROM t
    expected:
      rows:
        - ["[{a key, 2 value}, {b key, 1 value}, {c key, 3 value}]"]
  - desc: "functions-and-operators#973 L33489 MAP_ENTRIES_UNSORTED"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('b', 1), ('a', 2), ('c', 3)]) AS input_map)
      SELECT MAP_ENTRIES_UNSORTED(input_map) AS results
      FROM t
    expected:
      rows:
        - ["[{a key, 2 value}, {b key, 1 value}, {c key, 3 value}]"]
  - desc: "functions-and-operators#974 L33538 MAP_FILTER"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), (NULL, 3), ('c', 5)]) AS input_map)
      SELECT
        MAP_FILTER(input_map, (k, v) -> k IS NOT NULL) AS results_a,
        MAP_FILTER(input_map, (k, v) -> v < 4) AS results_b,
        MAP_FILTER(input_map, (k, v) -> FALSE) AS results_c
      FROM t
    expected:
      rows:
        - ["{\"a\": 1, \"c\": 5}","{NULL: 3, \"a\": 1}","{}"]
  - desc: "functions-and-operators#975 L33585 MAP_FROM_ARRAY"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), ('b', 2), ('c', 3)]) AS input_map)
      SELECT
        input_map['c'] AS value
      FROM t
    expected:
      rows:
        - ["3"]
  - desc: "functions-and-operators#976 L33638 MAP_GET"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 2), (NULL, 3)]) AS input_map)
      SELECT
        MAP_GET(input_map, 'a') AS results_a,
        MAP_GET(input_map, 'x') AS results_b,
        MAP_GET(input_map, 'x', -1) AS results_c,
        MAP_GET(input_map, NULL, -2) AS results_d,
      FROM t
    expected:
      rows:
        - ["2",null,"-1","3"]
  - desc: "functions-and-operators#977 L33681 MAP_INSERT"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), ('b', 2)]) AS input_map)
      SELECT
        MAP_INSERT(input_map, 'x', 4, 'y', 5) AS results_a,
      FROM t
    expected:
      rows:
        - ["{\"a\": 1, \"b\": 2, \"x\": 4, \"y\": 5}"]
  # Verified on BigQuery 2026-09-25.
  - desc: "functions-and-operators#978 L33698 MAP_INSERT"
    sql: |-
      -- Error: Key 'b' already exists in map
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), ('b', 2)]) AS input_map)
      SELECT
        MAP_INSERT(input_map, 'b', 99, 'x', 4) AS results_b,
      FROM t
    expected:
      error:
        contains: ""
  - desc: "functions-and-operators#979 L33736 MAP_INSERT_OR_REPLACE"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), ('b', 2)]) AS input_map)
      SELECT
        MAP_INSERT_OR_REPLACE(input_map, 'x', 4, 'y', 5) AS results_a,
        MAP_INSERT_OR_REPLACE(input_map, 'b', 99, 'x', 4) AS results_b,
      FROM t
    expected:
      rows:
        - ["{\"a\": 1, \"b\": 2, \"x\": 4, \"y\": 5}","{\"a\": 1, \"b\": 99, \"x\": 4}"]
  - desc: "functions-and-operators#980 L33776 MAP_KEYS_SORTED"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('b', 1), ('a', 2), ('c', 3)]) AS input_map)
      SELECT MAP_KEYS_SORTED(input_map) AS results
      FROM t
    expected:
      rows:
        - ["[a, b, c]"]
  - desc: "functions-and-operators#981 L33813 MAP_KEYS_UNSORTED"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('b', 1), ('a', 2), ('c', 3)]) AS input_map)
      SELECT MAP_KEYS_UNSORTED(input_map) AS results
      FROM t
    expected:
      rows:
        - ["[a, b, c]"]
  - desc: "functions-and-operators#982 L33866 MAP_REPLACE"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), ('b', 2), ('c', 3)]) AS input_map)
      SELECT
        MAP_REPLACE(
          input_map,
          'b', 40,
          'a', 50,
          'c', 60
        ) AS results,
      FROM t
    expected:
      rows:
        - ["{\"a\": 50, \"b\": 40, \"c\": 60}"]
  - desc: "functions-and-operators#983 L33888 MAP_REPLACE"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), ('b', 2), ('c', 3)]) AS input_map)
      SELECT
        MAP_REPLACE(
          input_map,
          'a',
          'c',
          (v) -> v + 100
        ) AS results
      FROM t
    expected:
      rows:
        - ["{\"a\": 101, \"b\": 2, \"c\": 103}"]
  # Verified on BigQuery 2026-09-25.
  - desc: "functions-and-operators#984 L33910 MAP_REPLACE"
    sql: |-
      -- Error: x doesn't exist in input_map.
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('a', 1), ('b', 2), ('c', 3)]) AS input_map)
      SELECT
        MAP_REPLACE(
          input_map,
          'x', 60
        ) AS results,
      FROM t
    expected:
      error:
        contains: ""
  - desc: "functions-and-operators#985 L33947 MAP_VALUES_SORTED"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('b', 2), ('a', 3), ('c', 1)]) AS input_map)
      SELECT MAP_VALUES_SORTED(input_map) AS results
      FROM t
    expected:
      rows:
        - ["[1, 2, 3]"]
  - desc: "functions-and-operators#986 L33985 MAP_VALUES_SORTED_BY_KEY"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('b', 1), ('a', 2), ('c', 3)]) AS input_map)
      SELECT MAP_VALUES_SORTED_BY_KEY(input_map) AS results
      FROM t
    expected:
      rows:
        - ["[2, 1, 3]"]
  - desc: "functions-and-operators#987 L34022 MAP_VALUES_UNSORTED"
    skip: "not_in_bigquery: MAP functions; BigQuery has no MAP type."
    sql: |-
      WITH t AS
        (SELECT MAP_FROM_ARRAY([('b', 1), ('a', 2), ('c', 3)]) AS input_map)
      SELECT MAP_VALUES_UNSORTED(input_map) AS results
      FROM t
    expected:
      rows:
        - ["[2, 1, 3]"]
  - desc: "functions-and-operators#1012 L37902 AI.IF"
    skip: "docs_artifact: The example reads a `news` table the docs never define."
    sql: |-
      -- Join tables based on semantic understanding.
      SELECT
        news.title, news.body, products.product_name
      FROM
        news
      JOIN products
        ON
          AI.IF(
            CONCAT(
              'Determine if the following news story is about product ',
              products.product_name,
              ': ',
              news.body))
      WHERE
        category = 'tech'
    expected:
      rows:
        - ["New Google Pixel Released","Google announced its newest Pixel smartphone with advanced AI features.","Pixel"]
  - desc: "functions-and-operators#1017 L38224 KMEANS"
    skip: "not_in_bigquery: A KMEANS table-valued function over an undefined table; not a BigQuery SQL function."
    sql: |-
      WITH Songs AS (
        SELECT 1 AS song_id, 'Bohemian Rhapsody' AS title, 'Queen' AS artist, 'Rock' AS genre, [0.1, 0.9, 0.2] AS song_embedding
        UNION ALL
        SELECT 2, 'Like a Rolling Stone' AS title, 'Bob Dylan' AS artist, 'Rock' AS genre, [0.2, 0.8, 0.3] AS song_embedding
        UNION ALL
        SELECT 3, 'Stairway to Heaven' AS title, 'Led Zeppelin' AS artist, 'Rock' AS genre, [0.15, 0.85, 0.25] AS song_embedding
        UNION ALL
        SELECT 4, 'What a Wonderful World' AS title, 'Louis Armstrong' AS artist, 'Jazz' AS genre, [0.8, 0.1, 0.9] AS song_embedding
        UNION ALL
        SELECT 5, 'So What' AS title, 'Miles Davis' AS artist, 'Jazz' AS genre, [0.9, 0.2, 0.8] AS song_embedding
        UNION ALL
        SELECT 6, 'Hallelujah' AS title, 'Leonard Cohen' AS artist, 'Folk' AS genre, [0.5, 0.5, 0.5] AS song_embedding
      ),
      Centroids AS (
        SELECT * FROM KMEANS(TABLE Songs, 'song_embedding', k => 3)
      )
      SELECT
        T.title,
        T.artist,
        T.genre,
        (
          SELECT c.cluster_id
          FROM Centroids AS c
          ORDER BY EUCLIDEAN_DISTANCE(T.song_embedding, c.cluster_vector)
          LIMIT 1
        ) AS closest_centroid_id
      FROM Songs AS T
      ORDER BY closest_centroid_id, T.title
    expected:
      rows:
        - ["Bohemian Rhapsody","Queen","Rock","1"]
        - ["Like a Rolling Stone","Bob Dylan","Rock","1"]
        - ["Stairway to Heaven","Led Zeppelin","Rock","1"]
        - ["Hallelujah","Leonard Cohen","Folk","2"]
        - ["So What","Miles Davis","Jazz","3"]
        - ["What a Wonderful World","Louis Armstrong","Jazz","3"]
  - desc: "functions-and-operators#1180 L46542 LOWER"
    skip: "docs_artifact: The example reads an `items` table the page defines for another section (three rows); the docs show one row."
    setup:
      - |-
        CREATE TABLE Items AS SELECT STRUCT('blue' AS color, 'round' AS shape) AS info UNION ALL
            SELECT STRUCT('blue', 'square') UNION ALL
            SELECT STRUCT('red', 'round')
    sql: |-
      SELECT
        LOWER('FOO BAR BAZ') AS example
      FROM items
    expected:
      rows:
        - ["foo bar baz"]
