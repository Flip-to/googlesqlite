# Generated by TestDocsExamples from google/googlesql@d82db99 docs/data-definition-language.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "data-definition-language#2 L852 CREATE CONSTANT"
    skip: "not_in_bigquery: CREATE CONSTANT; BigQuery has no named constants."
    sql: |-
      SELECT (DEFAULT_HEIGHT + 5) AS result
    expected:
      rows:
        - ["30"]
