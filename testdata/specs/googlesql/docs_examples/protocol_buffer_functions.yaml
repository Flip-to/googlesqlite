# Generated by TestDocsExamples from google/googlesql@d82db99 docs/protocol_buffer_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "protocol_buffer_functions#19 L1034 REPLACE_FIELDS"
    skip: "not_in_bigquery: Protocol buffers (NEW Book, REPLACE_FIELDS)."
    sql: |-
      SELECT REPLACE_FIELDS(
        NEW Book(
          "The Hummingbird" AS title,
          NEW BookDetails(10 AS chapters) AS details),
        "The Hummingbird II" AS title,
        11 AS details.chapters)
      AS proto
    expected:
      rows:
        - ["{title: \"The Hummingbird II\" details: {chapters: 11 }}"]
  - desc: "protocol_buffer_functions#20 L1052 REPLACE_FIELDS"
    skip: "not_in_bigquery: Protocol buffers (NEW Book, REPLACE_FIELDS)."
    sql: |-
      SELECT REPLACE_FIELDS(
        NEW Book("The Hummingbird" AS title,
          NEW BookDetails(10 AS chapters) AS details),
        ["A good read!", "Highly recommended."] AS reviews)
      AS proto
    expected:
      rows:
        - ["{title: \"The Hummingbird\" review: \"A good read\" review: \"Highly recommended.\""]
        - ["details: {chapters: 10 }}"]
      unordered: true
  - desc: "protocol_buffer_functions#21 L1069 REPLACE_FIELDS"
    skip: "not_in_bigquery: Protocol buffers (NEW Book, REPLACE_FIELDS)."
    sql: |-
      SELECT REPLACE_FIELDS(
        NEW Book("The Hummingbird" AS title,
          NEW BookDetails(10 AS chapters) AS details),
        NULL AS details)
      AS proto
    expected:
      rows:
        - ["{title: \"The Hummingbird\" }"]
  - desc: "protocol_buffer_functions#24 L1264 TO_PROTO"
    skip: "not_in_bigquery: TO_PROTO and google.protobuf.Duration."
    sql: |-
      SELECT TO_PROTO(INTERVAL "2:46:40.5" HOUR TO SECOND)
    expected:
      rows:
        - ["{seconds: 10000 nanos: 500000000}"]
