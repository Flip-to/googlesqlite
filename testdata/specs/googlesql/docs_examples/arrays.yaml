# Generated by TestDocsExamples from google/googlesql@d82db99 docs/arrays.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "arrays#1 L32 Accessing array elements"
    sql: |-
      WITH
        Sequences AS (
          SELECT [0, 1, 1, 2, 3, 5] AS some_numbers UNION ALL
          SELECT [2, 4, 8, 16, 32] UNION ALL
          SELECT [5, 10]
        )
      SELECT * FROM Sequences
    expected:
      rows:
        - [[0,1,1,2,3,5]]
        - [[2,4,8,16,32]]
        - [[5,10]]
      unordered: true
  - desc: "arrays#2 L57 Accessing array elements"
    setup:
      - |-
        CREATE TABLE Sequences AS SELECT [0, 1, 1, 2, 3, 5] AS some_numbers UNION ALL
            SELECT [2, 4, 8, 16, 32] UNION ALL
            SELECT [5, 10]
    sql: |-
      SELECT
        some_numbers,
        some_numbers[0] AS index_0,
        some_numbers[OFFSET(1)] AS offset_1,
        some_numbers[ORDINAL(1)] AS ordinal_1
      FROM Sequences
    expected:
      rows:
        - [[0,1,1,2,3,5],0,1,0]
        - [[2,4,8,16,32],2,4,2]
        - [[5,10],5,10,5]
      unordered: true
  - desc: "arrays#3 L82 Accessing array elements"
    setup:
      - |-
        CREATE TABLE Sequences AS SELECT [0, 1, 1, 2, 3, 5] AS some_numbers UNION ALL
            SELECT [2, 4, 8, 16, 32] UNION ALL
            SELECT [5, 10]
    sql: |-
      SELECT
        some_numbers,
        ARRAY_FIRST(some_numbers) AS first_element,
        ARRAY_LAST(some_numbers) AS last_element
      FROM Sequences
    expected:
      rows:
        - [[0,1,1,2,3,5],0,5]
        - [[2,4,8,16,32],2,32]
        - [[5,10],5,10]
      unordered: true
  - desc: "arrays#4 L101 Accessing array elements"
    sql: |-
      WITH
        Sequences AS (
          SELECT [0, 1, 1, 2, 3, 5] AS some_numbers
          UNION ALL
          SELECT [2, 4, 8, 16, 32]
          UNION ALL
          SELECT [] -- Empty array
        )
      SELECT
        some_numbers,
        ARRAY_LAST(some_numbers) AS last_element
      FROM Sequences
    expected:
      error:
        contains: ""
  - desc: "arrays#5 L121 Accessing array elements"
    sql: |-
      WITH
        Sequences AS (
          SELECT [0, 1, 1, 2, 3, 5] AS some_numbers
          UNION ALL
          SELECT [2, 4, 8, 16, 32]
          UNION ALL
          SELECT [] -- Empty array
        )
      SELECT
        some_numbers,
        SAFE.ARRAY_LAST(some_numbers) AS last_element
      FROM Sequences
    expected:
      rows:
        - [[0,1,1,2,3,5],5]
        - [[2,4,8,16,32],32]
        - [[],null]
      unordered: true
  - desc: "arrays#6 L148 Finding lengths"
    sql: |-
      WITH Sequences AS
        (SELECT [0, 1, 1, 2, 3, 5] AS some_numbers
         UNION ALL SELECT [2, 4, 8, 16, 32] AS some_numbers
         UNION ALL SELECT [5, 10] AS some_numbers)
      SELECT some_numbers,
             ARRAY_LENGTH(some_numbers) AS len
      FROM Sequences
    expected:
      rows:
        - [[0,1,1,2,3,5],6]
        - [[2,4,8,16,32],5]
        - [[5,10],2]
      unordered: true
  - desc: "arrays#7 L199 Flattening nested data into an array"
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
    sql: |-
      SELECT FLATTEN(items.color) AS colors
      FROM ItemsTable
    expected:
      rows:
        - [["red","green","orange"]]
  - desc: "arrays#8 L210 Flattening nested data into an array"
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
    sql: |-
      SELECT FLATTEN(items.inventory) AS inventory
      FROM ItemsTable
    expected:
      rows:
        - [[2,null,10]]
  - desc: "arrays#9 L221 Flattening nested data into an array"
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
    sql: |-
      SELECT FLATTEN(items.sales.prices) AS all_prices
      FROM ItemsTable
    expected:
      rows:
        - [[100,50,25,75,200]]
  - desc: "arrays#10 L232 Flattening nested data into an array"
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
    sql: |-
      SELECT FLATTEN(items.sales.prices[SAFE_OFFSET(1)]) AS second_prices
      FROM ItemsTable
    expected:
      rows:
        - [[50,null,null,null]]
  - desc: "arrays#11 L277 Flattening nested data into a table"
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
    sql: |-
      -- In the FROM clause (UNNEST used implicitly):
      SELECT colors
      FROM ItemsTable
      INNER JOIN ItemsTable.items.color AS colors
    expected:
      rows:
        - ["red"]
        - ["green"]
        - ["orange"]
      unordered: true
  - desc: "arrays#12 L302 Flattening nested data into a table"
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
    sql: |-
      -- In the FROM clause (UNNEST used implicitly):
      SELECT inventory
      FROM ItemsTable
      INNER JOIN ItemsTable.items.inventory AS inventory
    expected:
      rows:
        - [2]
        - [null]
        - [10]
      unordered: true
  - desc: "arrays#13 L327 Flattening nested data into a table"
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
    sql: |-
      -- In the FROM clause (UNNEST used implicitly):
      SELECT all_prices
      FROM ItemsTable
      INNER JOIN ItemsTable.items.sales.prices AS all_prices
    expected:
      rows:
        - [100]
        - [50]
        - [25]
        - [75]
        - [200]
      unordered: true
  - desc: "arrays#14 L354 Flattening nested data into a table"
    skip: "not_in_bigquery: Array subscript on a FROM-clause path without UNNEST; BigQuery rejects it with \"Array element access is not allowed in the FROM clause without UNNEST\"."
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
    sql: |-
      -- In the FROM clause (UNNEST used implicitly):
      SELECT second_prices
      FROM ItemsTable
      INNER JOIN ItemsTable.items.sales.prices[SAFE_OFFSET(1)] AS second_prices
    expected:
      rows:
        - ["50"]
        - [null]
        - [null]
        - [null]
      unordered: true
  - desc: "arrays#15 L395 Converting elements in an array to rows in a table"
    sql: |-
      SELECT *
      FROM UNNEST(['foo', 'bar', 'baz', 'qux', 'corge', 'garply', 'waldo', 'fred'])
        AS element
      WITH OFFSET AS offset
      ORDER BY offset
    expected:
      rows:
        - ["foo",0]
        - ["bar",1]
        - ["baz",2]
        - ["qux",3]
        - ["corge",4]
        - ["garply",5]
        - ["waldo",6]
        - ["fred",7]
  - desc: "arrays#16 L434 Converting elements in an array to rows in a table"
    sql: |-
      WITH
        Sequences AS (
          SELECT 1 AS id, [0, 1, 1, 2, 3, 5] AS some_numbers
          UNION ALL SELECT 2 AS id, [2, 4, 8, 16, 32] AS some_numbers
          UNION ALL SELECT 3 AS id, [5, 10] AS some_numbers
        )
      SELECT id, flattened_numbers
      FROM Sequences
      INNER JOIN UNNEST(Sequences.some_numbers) AS flattened_numbers
    expected:
      rows:
        - [1,0]
        - [1,1]
        - [1,1]
        - [1,2]
        - [1,3]
        - [1,5]
        - [2,2]
        - [2,4]
        - [2,8]
        - [2,16]
        - [2,32]
        - [3,5]
        - [3,10]
      unordered: true
  - desc: "arrays#17 L469 Converting elements in an array to rows in a table"
    sql: |-
      WITH
        Sequences AS (
          SELECT 1 AS id, [0, 1, 1, 2, 3, 5] AS some_numbers
          UNION ALL SELECT 2 AS id, [2, 4, 8, 16, 32] AS some_numbers
          UNION ALL SELECT 3 AS id, [5, 10] AS some_numbers
        )
      SELECT id, flattened_numbers
      FROM Sequences, Sequences.some_numbers AS flattened_numbers
    expected:
      rows:
        - [1,0]
        - [1,1]
        - [1,1]
        - [1,2]
        - [1,3]
        - [1,5]
        - [2,2]
        - [2,4]
        - [2,8]
        - [2,16]
        - [2,32]
        - [3,5]
        - [3,10]
      unordered: true
  - desc: "arrays#18 L513 Querying `STRUCT` elements in an array"
    sql: |-
      WITH
        Races AS (
          SELECT
            "800M" AS race,
            [
              STRUCT("Rudisha" AS name, [23.4, 26.3, 26.4, 26.1] AS laps),
              STRUCT("Makhloufi" AS name, [24.5, 25.4, 26.6, 26.1] AS laps),
              STRUCT("Murphy" AS name, [23.9, 26.0, 27.0, 26.0] AS laps),
              STRUCT("Bosse" AS name, [23.6, 26.2, 26.5, 27.1] AS laps),
              STRUCT("Rotich" AS name, [24.7, 25.6, 26.9, 26.4] AS laps),
              STRUCT("Lewandowski" AS name, [25.0, 25.7, 26.3, 27.2] AS laps),
              STRUCT("Kipketer" AS name, [23.2, 26.1, 27.3, 29.4] AS laps),
              STRUCT("Berian" AS name, [23.7, 26.1, 27.0, 29.3] AS laps)
            ] AS participants
          )
      SELECT
        race,
        participant
      FROM Races AS r
      INNER JOIN UNNEST(r.participants) AS participant
    expected:
      rows:
        - ["800M",["Rudisha",[23.4,26.3,26.4,26.1]]]
        - ["800M",["Makhloufi",[24.5,25.4,26.6,26.1]]]
        - ["800M",["Murphy",[23.9,26,27,26]]]
        - ["800M",["Bosse",[23.6,26.2,26.5,27.1]]]
        - ["800M",["Rotich",[24.7,25.6,26.9,26.4]]]
        - ["800M",["Lewandowski",[25,25.7,26.3,27.2]]]
        - ["800M",["Kipketer",[23.2,26.1,27.3,29.4]]]
        - ["800M",["Berian",[23.7,26.1,27,29.3]]]
      unordered: true
  - desc: "arrays#19 L557 Querying `STRUCT` elements in an array"
    sql: |-
      WITH
        Races AS (
          SELECT
            "800M" AS race,
            [
              STRUCT("Rudisha" AS name, [23.4, 26.3, 26.4, 26.1] AS laps),
              STRUCT("Makhloufi" AS name, [24.5, 25.4, 26.6, 26.1] AS laps),
              STRUCT("Murphy" AS name, [23.9, 26.0, 27.0, 26.0] AS laps),
              STRUCT("Bosse" AS name, [23.6, 26.2, 26.5, 27.1] AS laps),
              STRUCT("Rotich" AS name, [24.7, 25.6, 26.9, 26.4] AS laps),
              STRUCT("Lewandowski" AS name, [25.0, 25.7, 26.3, 27.2] AS laps),
              STRUCT("Kipketer" AS name, [23.2, 26.1, 27.3, 29.4] AS laps),
              STRUCT("Berian" AS name, [23.7, 26.1, 27.0, 29.3] AS laps)
            ] AS participants
        )
      SELECT
        race,
        (
          SELECT name
          FROM UNNEST(participants)
          ORDER BY (SELECT SUM(duration) FROM UNNEST(laps) AS duration) ASC
          LIMIT 1
        ) AS fastest_racer
      FROM Races
    expected:
      rows:
        - ["800M","Rudisha"]
  - desc: "arrays#22 L691 Querying `ARRAY`-type fields in a struct"
    sql: |-
      WITH
        Races AS (
          SELECT
            "800M" AS race,
            [
              STRUCT("Rudisha" AS name, [23.4, 26.3, 26.4, 26.1] AS laps),
              STRUCT("Makhloufi" AS name, [24.5, 25.4, 26.6, 26.1] AS laps),
              STRUCT("Murphy" AS name, [23.9, 26.0, 27.0, 26.0] AS laps),
              STRUCT("Bosse" AS name, [23.6, 26.2, 26.5, 27.1] AS laps),
              STRUCT("Rotich" AS name, [24.7, 25.6, 26.9, 26.4] AS laps),
              STRUCT("Lewandowski" AS name, [25.0, 25.7, 26.3, 27.2] AS laps),
              STRUCT("Kipketer" AS name, [23.2, 26.1, 27.3, 29.4] AS laps),
              STRUCT("Berian" AS name, [23.7, 26.1, 27.0, 29.3] AS laps)
            ]AS participants
        )
      SELECT
        race,
        (
          SELECT name
          FROM UNNEST(participants), UNNEST(laps) AS duration
          ORDER BY duration ASC
          LIMIT 1
        ) AS runner_with_fastest_lap
      FROM Races
    expected:
      rows:
        - ["800M","Kipketer"]
  - desc: "arrays#23 L728 Querying `ARRAY`-type fields in a struct"
    sql: |-
      WITH
        Races AS (
          SELECT "800M" AS race,
            [
              STRUCT("Rudisha" AS name, [23.4, 26.3, 26.4, 26.1] AS laps),
              STRUCT("Makhloufi" AS name, [24.5, 25.4, 26.6, 26.1] AS laps),
              STRUCT("Murphy" AS name, [23.9, 26.0, 27.0, 26.0] AS laps),
              STRUCT("Bosse" AS name, [23.6, 26.2, 26.5, 27.1] AS laps),
              STRUCT("Rotich" AS name, [24.7, 25.6, 26.9, 26.4] AS laps),
              STRUCT("Lewandowski" AS name, [25.0, 25.7, 26.3, 27.2] AS laps),
              STRUCT("Kipketer" AS name, [23.2, 26.1, 27.3, 29.4] AS laps),
              STRUCT("Berian" AS name, [23.7, 26.1, 27.0, 29.3] AS laps)
            ] AS participants
        )
      SELECT
        race,
        (
          SELECT name
          FROM UNNEST(participants)
          INNER JOIN UNNEST(laps) AS duration
          ORDER BY duration ASC LIMIT 1
        ) AS runner_with_fastest_lap
      FROM Races
    expected:
      rows:
        - ["800M","Kipketer"]
  - desc: "arrays#24 L763 Querying `ARRAY`-type fields in a struct"
    sql: |-
      WITH
        Races AS (
          SELECT
            "800M" AS race,
            [
              STRUCT("Rudisha" AS name, [23.4, 26.3, 26.4, 26.1] AS laps),
              STRUCT("Makhloufi" AS name, [24.5, 25.4, 26.6, 26.1] AS laps),
              STRUCT("Murphy" AS name, [23.9, 26.0, 27.0, 26.0] AS laps),
              STRUCT("Bosse" AS name, [23.6, 26.2, 26.5, 27.1] AS laps),
              STRUCT("Rotich" AS name, [24.7, 25.6, 26.9, 26.4] AS laps),
              STRUCT("Lewandowski" AS name, [25.0, 25.7, 26.3, 27.2] AS laps),
              STRUCT("Kipketer" AS name, [23.2, 26.1, 27.3, 29.4] AS laps),
              STRUCT("Berian" AS name, [23.7, 26.1, 27.0, 29.3] AS laps),
              STRUCT("Nathan" AS name, ARRAY<DOUBLE>[] AS laps),
              STRUCT("David" AS name, NULL AS laps)
            ] AS participants
        )
      SELECT
        Participant.name,
        SUM(duration) AS finish_time
      FROM Races
      INNER JOIN Races.participants AS Participant
      LEFT JOIN Participant.laps AS duration
      GROUP BY name
    expected:
      rows:
        - ["Murphy",102.9]
        - ["Rudisha",102.19999999999999]
        - ["David",null]
        - ["Rotich",103.6]
        - ["Makhloufi",102.6]
        - ["Berian",106.1]
        - ["Bosse",103.4]
        - ["Kipketer",106]
        - ["Nathan",null]
        - ["Lewandowski",104.2]
      unordered: true
  - desc: "arrays#27 L899 Casting arrays"
    skip: "not_in_bigquery: ARRAY\u003cINT32\u003e; BigQuery has no INT32 type."
    sql: |-
      SELECT CAST(int_array AS ARRAY<DOUBLE>) AS double_array
      FROM (SELECT ARRAY<INT32>[1, 2, 3] AS int_array)
    expected:
      rows:
        - ["[1, 2, 3]"]
  - desc: "arrays#28 L923 Creating arrays from subqueries"
    sql: |-
      WITH Sequences AS
        (SELECT [0, 1, 1, 2, 3, 5] AS some_numbers
        UNION ALL SELECT [2, 4, 8, 16, 32] AS some_numbers
        UNION ALL SELECT [5, 10] AS some_numbers)
      SELECT some_numbers,
        ARRAY(SELECT x * 2
              FROM UNNEST(some_numbers) AS x) AS doubled
      FROM Sequences
    expected:
      rows:
        - [[0,1,1,2,3,5],[0,2,2,4,6,10]]
        - [[2,4,8,16,32],[4,8,16,32,64]]
        - [[5,10],[10,20]]
      unordered: true
  - desc: "arrays#29 L958 Filtering arrays"
    sql: |-
      WITH Sequences AS
        (SELECT [0, 1, 1, 2, 3, 5] AS some_numbers
         UNION ALL SELECT [2, 4, 8, 16, 32] AS some_numbers
         UNION ALL SELECT [5, 10] AS some_numbers)
      SELECT
        ARRAY(SELECT x * 2
              FROM UNNEST(some_numbers) AS x
              WHERE x < 5) AS doubled_less_than_five
      FROM Sequences
    expected:
      rows:
        - [[0,2,2,4,6]]
        - [[4,8]]
        - [[]]
      unordered: true
  - desc: "arrays#30 L985 Filtering arrays"
    sql: |-
      WITH Sequences AS
        (SELECT [0, 1, 1, 2, 3, 5] AS some_numbers)
      SELECT ARRAY(SELECT DISTINCT x
                   FROM UNNEST(some_numbers) AS x) AS unique_numbers
      FROM Sequences
    expected:
      rows:
        - [[0,1,2,3,5]]
  - desc: "arrays#31 L1004 Filtering arrays"
    sql: |-
      WITH Sequences AS
        (SELECT [0, 1, 1, 2, 3, 5] AS some_numbers
         UNION ALL SELECT [2, 4, 8, 16, 32] AS some_numbers
         UNION ALL SELECT [5, 10] AS some_numbers)
      SELECT
         ARRAY(SELECT x
               FROM UNNEST(some_numbers) AS x
               WHERE 2 IN UNNEST(some_numbers)) AS contains_two
      FROM Sequences
    expected:
      rows:
        - [[0,1,1,2,3,5]]
        - [[2,4,8,16,32]]
        - [[]]
      unordered: true
  - desc: "arrays#32 L1042 Scanning for specific values"
    sql: |-
      SELECT 2 IN UNNEST([0, 1, 1, 2, 3, 5]) AS contains_value
    expected:
      rows:
        - [true]
  - desc: "arrays#33 L1060 Scanning for specific values"
    sql: |-
      WITH Sequences AS
        (SELECT 1 AS id, [0, 1, 1, 2, 3, 5] AS some_numbers
         UNION ALL SELECT 2 AS id, [2, 4, 8, 16, 32] AS some_numbers
         UNION ALL SELECT 3 AS id, [5, 10] AS some_numbers)
      SELECT id AS matching_rows
      FROM Sequences
      WHERE 2 IN UNNEST(Sequences.some_numbers)
      ORDER BY matching_rows
    expected:
      rows:
        - [1]
        - [2]
  - desc: "arrays#37 L1166 Arrays and aggregation"
    sql: |-
      WITH Fruits AS
        (SELECT "apple" AS fruit
         UNION ALL SELECT "pear" AS fruit
         UNION ALL SELECT "banana" AS fruit)
      SELECT ARRAY_AGG(fruit ORDER BY fruit) AS fruit_basket
      FROM Fruits
    expected:
      rows:
        - [["apple","banana","pear"]]
  - desc: "arrays#38 L1185 Arrays and aggregation"
    sql: |-
      WITH Sequences AS
        (SELECT [0, 1, 1, 2, 3, 5] AS some_numbers
         UNION ALL SELECT [2, 4, 8, 16, 32] AS some_numbers
         UNION ALL SELECT [5, 10] AS some_numbers)
      SELECT some_numbers,
        (SELECT SUM(x)
         FROM UNNEST(s.some_numbers) AS x) AS sums
      FROM Sequences AS s
    expected:
      rows:
        - [[0,1,1,2,3,5],12]
        - [[2,4,8,16,32],62]
        - [[5,10],15]
      unordered: true
  - desc: "arrays#40 L1238 Converting arrays to strings"
    sql: |-
      WITH Words AS
        (SELECT ["Hello", "World"] AS greeting)
      SELECT ARRAY_TO_STRING(greeting, " ") AS greetings
      FROM Words
    expected:
      rows:
        - ["Hello World"]
  - desc: "arrays#41 L1262 Converting arrays to strings"
    sql: |-
      SELECT
        ARRAY_TO_STRING(arr, ".", "N") AS non_empty_string,
        ARRAY_TO_STRING(arr, ".", "") AS empty_string,
        ARRAY_TO_STRING(arr, ".") AS omitted
      FROM (SELECT ["a", NULL, "b", NULL, "c", NULL] AS arr)
    expected:
      rows:
        - ["a.N.b.N.c.N","a..b..c.","a.b.c"]
