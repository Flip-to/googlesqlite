# Generated by TestDocsExamples from google/googlesql@d82db99 docs/data-model.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  # Verified on BigQuery 2026-09-25.
  - desc: "data-model#1 L233 Value tables"
    sql: |-
      SELECT * FROM (SELECT AS VALUE STRUCT(123 AS a, FALSE AS b))
    expected:
      rows:
        - [123,false]
  # Verified on BigQuery 2026-09-25.
  - desc: "data-model#3 L437 Pseudocolumns and value tables"
    sql: |-
      -- This fails
      SELECT a.ROWNUM, a.albumtitle AS title FROM (SELECT a FROM AlbumReviewData AS a)
    expected:
      error:
        contains: ""
