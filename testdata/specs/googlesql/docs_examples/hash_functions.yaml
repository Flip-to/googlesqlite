# Generated by TestDocsExamples from google/googlesql@d82db99 docs/hash_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "hash_functions#1 L98 FARM_FINGERPRINT"
    sql: |-
      WITH example AS (
        SELECT 1 AS x, "foo" AS y, true AS z UNION ALL
        SELECT 2 AS x, "apple" AS y, false AS z UNION ALL
        SELECT 3 AS x, "" AS y, true AS z
      )
      SELECT
        *,
        FARM_FINGERPRINT(CONCAT(CAST(x AS STRING), y, CAST(z AS STRING)))
          AS row_fingerprint
      FROM example
    expected:
      rows:
        - [1,"foo",true,-1541654101129638711]
        - [2,"apple",false,2794438866806483259]
        - [3,"",true,-4880158226897771312]
      unordered: true
  - desc: "hash_functions#2 L152 HIGHWAY_FINGERPRINT128"
    skip: "not_in_bigquery: HIGHWAY_FINGERPRINT128; BigQuery raises \"Function not found\"."
    sql: |-
      -- Without `key`

      SELECT HIGHWAY_FINGERPRINT128('Hello World')
    expected:
      rows:
        - ["o8O4tP5viFe/iK6J5KvTjg=="]
  - desc: "hash_functions#3 L163 HIGHWAY_FINGERPRINT128"
    skip: "not_in_bigquery: HIGHWAY_FINGERPRINT128; BigQuery raises \"Function not found\"."
    sql: |-
      -- With `key`

      SELECT HIGHWAY_FINGERPRINT128(
          'Hello World',
          -- 32-byte key
          b'abcdefghijklmnopqrstuvwxyzabcdef'
      )
    expected:
      rows: []
  - desc: "hash_functions#4 L203 MD5"
    sql: |-
      SELECT MD5("Hello World") as md5
    expected:
      rows:
        - ["sQqNsWTgdUEFt6mb5y4/5Q=="]
  - desc: "hash_functions#5 L238 SHA1"
    sql: |-
      SELECT SHA1("Hello World") as sha1
    expected:
      rows:
        - ["Ck1VqNd45QIvq3AZd8XYQLvEhtA="]
