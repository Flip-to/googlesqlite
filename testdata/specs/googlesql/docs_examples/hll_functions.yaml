# Generated by TestDocsExamples from google/googlesql@d82db99 docs/hll_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "hll_functions#1 L102 HLL_COUNT.EXTRACT"
    sql: |-
      SELECT
        country,
        HLL_COUNT.EXTRACT(HLL_sketch) AS distinct_customers_with_open_invoice
      FROM
        (
          SELECT
            country,
            HLL_COUNT.INIT(customer_id) AS hll_sketch
          FROM
            UNNEST(
              ARRAY<STRUCT<country STRING, customer_id STRING, invoice_id STRING>>[
                ('UA', 'customer_id_1', 'invoice_id_11'),
                ('BR', 'customer_id_3', 'invoice_id_31'),
                ('CZ', 'customer_id_2', 'invoice_id_22'),
                ('CZ', 'customer_id_2', 'invoice_id_23'),
                ('BR', 'customer_id_3', 'invoice_id_31'),
                ('UA', 'customer_id_2', 'invoice_id_24')])
          GROUP BY country
        )
    expected:
      rows:
        - ["UA",2]
        - ["BR",1]
        - ["CZ",1]
      unordered: true
  - desc: "hll_functions#2 L185 HLL_COUNT.INIT"
    skip: "nondeterministic: The documented sketch bytes are an implementation-specific encoding (a GoogleSQL proto text dump); BigQuery and this driver produce different, equally valid sketches."
    sql: |-
      SELECT
        country,
        HLL_COUNT.INIT(customer_id, 10)
          AS hll_sketch
      FROM
        UNNEST(
          ARRAY<STRUCT<country STRING, customer_id STRING, invoice_id STRING>>[
            ('UA', 'customer_id_1', 'invoice_id_11'),
            ('CZ', 'customer_id_2', 'invoice_id_22'),
            ('CZ', 'customer_id_2', 'invoice_id_23'),
            ('BR', 'customer_id_3', 'invoice_id_31'),
            ('UA', 'customer_id_2', 'invoice_id_24')])
      GROUP BY country
    expected:
      rows:
        - ["UA","CHAQAhgCIAuCBw0QAhgKIA8yBfnkAc0I"]
        - ["CZ","CHAQAhgCIAuCBwsQARgKIA8yA/nkAQ=="]
        - ["BR","CHAQARgCIAuCBwsQARgKIA8yA4LhAQ=="]
      unordered: true
