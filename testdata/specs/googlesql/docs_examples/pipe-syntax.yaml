# Generated by TestDocsExamples from google/googlesql@d82db99 docs/pipe-syntax.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "pipe-syntax#1 L40 Query comparison"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      SELECT * FROM Produce
    expected:
      rows:
        - ["apples",2,"fruit"]
        - ["carrots",8,"vegetable"]
        - ["apples",7,"fruit"]
        - ["bananas",5,"fruit"]
      unordered: true
  - desc: "pipe-syntax#2 L68 Query comparison"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      SELECT item, COUNT(*) AS num_items, SUM(sales) AS total_sales
      FROM Produce
      WHERE
        item != 'bananas'
        AND category IN ('fruit', 'nut')
      GROUP BY item
      ORDER BY item DESC
    expected:
      rows:
        - ["apples",2,9]
  - desc: "pipe-syntax#3 L86 Query comparison"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      FROM Produce
      |> WHERE
          item != 'bananas'
          AND category IN ('fruit', 'nut')
      |> AGGREGATE COUNT(*) AS num_items, SUM(sales) AS total_sales
         GROUP BY item
      |> ORDER BY item DESC
    expected:
      rows:
        - ["apples",2,9]
  - desc: "pipe-syntax#4 L245 FROM` queries"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      FROM Produce
    expected:
      rows:
        - ["apples",2,"fruit"]
        - ["carrots",8,"vegetable"]
        - ["apples",7,"fruit"]
        - ["bananas",5,"fruit"]
      unordered: true
  - desc: "pipe-syntax#5 L258 FROM` queries"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      -- Join tables in the FROM clause and then apply pipe operators.
      FROM
        Produce AS p1
        JOIN Produce AS p2
          USING (item)
      |> WHERE item = 'bananas'
      |> SELECT p1.item, p2.sales
    expected:
      rows:
        - ["bananas",5]
  - desc: "pipe-syntax#7 L582 SELECT` pipe operator"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      -- Window function with a named window
      FROM Produce
      |> SELECT item, sales, category, SUM(sales) OVER item_window AS category_total
         WINDOW item_window AS (PARTITION BY category)
    expected:
      rows:
        - ["apples",2,"fruit",14]
        - ["apples",7,"fruit",14]
        - ["bananas",5,"fruit",14]
        - ["carrots",8,"vegetable",8]
      unordered: true
  - desc: "pipe-syntax#8 L636 EXTEND` pipe operator"
    sql: |-
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 8 AS sales
      )
      |> EXTEND item IN ('bananas', 'lemons') AS is_yellow
    expected:
      rows:
        - ["apples",2,false]
        - ["bananas",8,true]
      unordered: true
  - desc: "pipe-syntax#9 L652 EXTEND` pipe operator"
    sql: |-
      -- Window function, with `OVER`
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'carrots' AS item, 8 AS sales
      )
      |> EXTEND SUM(sales) OVER() AS total_sales
    expected:
      rows:
        - ["apples",2,15]
        - ["bananas",5,15]
        - ["carrots",8,15]
      unordered: true
  - desc: "pipe-syntax#10 L672 EXTEND` pipe operator"
    skip: "docs_artifact: The documented table leaves out the sales column that |\u003e EXTEND keeps (the docs show item, category, category_total)."
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      -- Window function with a named window
      FROM Produce
      |> EXTEND SUM(sales) OVER item_window AS category_total
         WINDOW item_window AS (PARTITION BY category)
    expected:
      rows:
        - ["apples","fruit","14"]
        - ["apples","fruit","14"]
        - ["bananas","fruit","14"]
        - ["carrots","vegetable","8"]
      unordered: true
  - desc: "pipe-syntax#11 L713 SET` pipe operator"
    sql: |-
      (
        SELECT 1 AS x, 11 AS y
        UNION ALL
        SELECT 2 AS x, 22 AS y
      )
      |> SET x = x * x, y = 3
    expected:
      rows:
        - [1,3]
        - [4,3]
      unordered: true
  - desc: "pipe-syntax#12 L729 SET` pipe operator"
    sql: |-
      FROM (SELECT 2 AS x, 3 AS y) AS t
      |> SET x = x * x, y = 8
      |> SELECT t.x AS original_x, x, y
    expected:
      rows:
        - [2,4,8]
  - desc: "pipe-syntax#13 L766 DROP` pipe operator"
    sql: |-
      SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
      |> DROP sales, category
    expected:
      rows:
        - ["apples"]
  - desc: "pipe-syntax#14 L777 DROP` pipe operator"
    sql: |-
      FROM (SELECT 1 AS x, 2 AS y) AS t
      |> DROP x
      |> SELECT t.x AS original_x, y
    expected:
      rows:
        - [1,2]
  - desc: "pipe-syntax#15 L813 RENAME` pipe operator"
    sql: |-
      SELECT 1 AS x, 2 AS y, 3 AS z
      |> AS t
      |> RENAME y AS renamed_y
      |> SELECT *, t.y AS t_y
    expected:
      rows:
        - [1,2,3,2]
  - desc: "pipe-syntax#16 L848 AS` pipe operator"
    sql: |-
      (
        SELECT "000123" AS id, "apples" AS item, 2 AS sales
        UNION ALL
        SELECT "000456" AS id, "bananas" AS item, 5 AS sales
      ) AS sales_table
      |> AGGREGATE SUM(sales) AS total_sales GROUP BY id, item
      -- AGGREGATE creates an output table, so the sales_table alias is now out of
      -- scope. Add a t1 alias so the join can refer to its id column.
      |> AS t1
      |> JOIN (SELECT 456 AS id, "yellow" AS color) AS t2
         ON CAST(t1.id AS INT64) = t2.id
      |> SELECT t2.id, total_sales, color
    expected:
      rows:
        - [456,5,"yellow"]
  - desc: "pipe-syntax#17 L898 WHERE` pipe operator"
    sql: |-
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'carrots' AS item, 8 AS sales
      )
      |> WHERE sales >= 3
    expected:
      rows:
        - ["bananas",5]
        - ["carrots",8]
      unordered: true
  - desc: "pipe-syntax#18 L1009 AGGREGATE` pipe operator"
    sql: |-
      -- Full-table aggregation
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'apples' AS item, 7 AS sales
      )
      |> AGGREGATE COUNT(*) AS num_items, SUM(sales) AS total_sales
    expected:
      rows:
        - [3,14]
  - desc: "pipe-syntax#19 L1027 AGGREGATE` pipe operator"
    sql: |-
      -- Aggregation with grouping
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'apples' AS item, 7 AS sales
      )
      |> AGGREGATE COUNT(*) AS num_items, SUM(sales) AS total_sales
         GROUP BY item
    expected:
      rows:
        - ["apples",2,9]
        - ["bananas",1,5]
      unordered: true
  - desc: "pipe-syntax#20 L1094 Shorthand ordering syntax with `AGGREGATE"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      -- Order by all grouping columns using GROUP AND ORDER BY.
      FROM Produce
      |> AGGREGATE SUM(sales) AS total_sales
         GROUP AND ORDER BY category, item DESC
    expected:
      rows:
        - ["fruit","bananas",5]
        - ["fruit","apples",9]
        - ["vegetable","carrots",8]
  - desc: "pipe-syntax#21 L1121 Shorthand ordering syntax with `AGGREGATE"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      FROM Produce
      |> AGGREGATE SUM(sales) AS total_sales ASC
         GROUP BY item, category DESC
    expected:
      rows:
        - ["carrots","vegetable",8]
        - ["bananas","fruit",5]
        - ["apples","fruit",9]
      unordered: true
  - desc: "pipe-syntax#22 L1178 DISTINCT` pipe operator"
    sql: |-
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'carrots' AS item, 8 AS sales
      )
      |> DISTINCT
      |> WHERE sales >= 3
    expected:
      rows:
        - ["bananas",5]
        - ["carrots",8]
      unordered: true
  - desc: "pipe-syntax#23 L1202 DISTINCT` pipe operator"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'carrots' AS item, 8 AS sales
      )
      |> AS Produce
      |> DISTINCT
      |> SELECT Produce.item
    expected:
      rows:
        - ["apples"]
        - ["bananas"]
        - ["carrots"]
      unordered: true
  - desc: "pipe-syntax#24 L1228 DISTINCT` pipe operator"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      -- Error, unrecognized name: Produce
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'carrots' AS item, 8 AS sales
      )
      |> AS Produce
      |> SELECT DISTINCT *
      |> SELECT Produce.item
    expected:
      error:
        contains: ""
  - desc: "pipe-syntax#26 L1262 DISTINCT` pipe operator"
    sql: |-
      SELECT AS STRUCT 1 x, 2 y
      |> SELECT DISTINCT *
    expected:
      rows:
        - [1,2]
  - desc: "pipe-syntax#27 L1326 JOIN` pipe operator"
    sql: |-
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
      )
      |> AS produce_sales
      |> LEFT JOIN
           (
             SELECT "apples" AS item, 123 AS id
           ) AS produce_data
         ON produce_sales.item = produce_data.item
      |> SELECT produce_sales.item, sales, id
    expected:
      rows:
        - ["apples",2,123]
        - ["bananas",5,null]
      unordered: true
  - desc: "pipe-syntax#28 L1475 ORDER BY` pipe operator"
    sql: |-
      (
        SELECT 1 AS x
        UNION ALL
        SELECT 3 AS x
        UNION ALL
        SELECT 2 AS x
      )
      |> ORDER BY x DESC
    expected:
      rows:
        - [3]
        - [2]
        - [1]
  - desc: "pipe-syntax#29 L1516 LIMIT` pipe operator"
    sql: |-
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'carrots' AS item, 8 AS sales
      )
      |> ORDER BY item
      |> LIMIT 1
    expected:
      rows:
        - ["apples",2]
  - desc: "pipe-syntax#30 L1534 LIMIT` pipe operator"
    sql: |-
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'carrots' AS item, 8 AS sales
      )
      |> ORDER BY item
      |> LIMIT 1 OFFSET 2
    expected:
      rows:
        - ["carrots",8]
  - desc: "pipe-syntax#31 L1596 UNION` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3]) AS number
      |> UNION ALL (SELECT 1)
    expected:
      rows:
        - [1]
        - [2]
        - [3]
        - [1]
      unordered: true
  - desc: "pipe-syntax#32 L1610 UNION` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3]) AS number
      |> UNION DISTINCT (SELECT 1)
    expected:
      rows:
        - [1]
        - [2]
        - [3]
      unordered: true
  - desc: "pipe-syntax#33 L1626 UNION` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3]) AS number
      |> UNION DISTINCT
          (SELECT 1),
          (SELECT 2)
    expected:
      rows:
        - [1]
        - [2]
        - [3]
      unordered: true
  - desc: "pipe-syntax#34 L1645 UNION` pipe operator"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit
      |> UNION ALL BY NAME
          (SELECT 20 AS two_digit, 2 AS one_digit)
    expected:
      rows:
        - [1,10]
        - [2,20]
      unordered: true
  - desc: "pipe-syntax#35 L1662 UNION` pipe operator"
    sql: |-
      SELECT 1 AS one_digit, 10 AS two_digit
      |> UNION ALL
          (SELECT 20 AS two_digit, 2 AS one_digit)
    expected:
      rows:
        - [1,10]
        - [20,2]
      unordered: true
  - desc: "pipe-syntax#36 L1803 RECURSIVE UNION` pipe operator"
    skip: "not_in_bigquery: Pipe RECURSIVE UNION; BigQuery raises \"Pipe RECURSIVE UNION is not supported\"."
    sql: |-
      CREATE TEMP TABLE Employees(
        employee_id INT64,
        manager_id INT64,
        state STRING);
      FROM Employees
      |> WHERE employee_id = 123456
      |> RECURSIVE UNION ALL
         (
           |> JOIN Employees e ON e.manager_id = AllReportees.employee_id
           |> SELECT e.*
         ) AS AllReportees
      |> WHERE State = 'AK'
      |> AGGREGATE COUNT(*) AS num_employees
    expected:
      rows:
        - ["46"]
  - desc: "pipe-syntax#37 L1830 RECURSIVE UNION` pipe operator"
    skip: "not_in_bigquery: Pipe RECURSIVE UNION; BigQuery raises \"Pipe RECURSIVE UNION is not supported\"."
    sql: |-
      CREATE TEMP TABLE Employees(
        employee_id INT64,
        manager_id INT64,
        state STRING);
      FROM Employees
      |> WHERE employee_id = 123456
      |> RECURSIVE UNION ALL
      (
        FROM AllReportees r  -- Recursive input table
        |> JOIN Employees e ON e.manager_id = r.employee_id
        |> SELECT e.*
      ) AS AllReportees
      |> WHERE State = 'AK'
      |> AGGREGATE COUNT(*) AS num_employees
    expected:
      rows:
        - ["46"]
  - desc: "pipe-syntax#38 L1904 INTERSECT` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      |> INTERSECT ALL
          (SELECT * FROM UNNEST(ARRAY<INT64>[2, 3, 3, 5]) AS number)
    expected:
      rows:
        - [2]
        - [3]
        - [3]
      unordered: true
  - desc: "pipe-syntax#39 L1918 INTERSECT` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      |> INTERSECT DISTINCT
          (SELECT * FROM UNNEST(ARRAY<INT64>[2, 3, 3, 5]) AS number)
    expected:
      rows:
        - [2]
        - [3]
      unordered: true
  - desc: "pipe-syntax#40 L1934 INTERSECT` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      |> INTERSECT DISTINCT
          (SELECT * FROM UNNEST(ARRAY<INT64>[2, 3, 3, 5]) AS number),
          (SELECT * FROM UNNEST(ARRAY<INT64>[3, 3, 4, 5]) AS number)
    expected:
      rows:
        - [3]
  - desc: "pipe-syntax#43 L2070 EXCEPT` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      |> EXCEPT ALL
          (SELECT * FROM UNNEST(ARRAY<INT64>[1, 2]) AS number)
    expected:
      rows:
        - [3]
        - [3]
        - [4]
      unordered: true
  - desc: "pipe-syntax#44 L2084 EXCEPT` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      |> EXCEPT DISTINCT
          (SELECT * FROM UNNEST(ARRAY<INT64>[1, 2]) AS number)
    expected:
      rows:
        - [3]
        - [4]
      unordered: true
  - desc: "pipe-syntax#45 L2100 EXCEPT` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      |> EXCEPT DISTINCT
          (SELECT * FROM UNNEST(ARRAY<INT64>[1, 2]) AS number),
          (SELECT * FROM UNNEST(ARRAY<INT64>[1, 4]) AS number)
    expected:
      rows:
        - [3]
  - desc: "pipe-syntax#46 L2117 EXCEPT` pipe operator"
    sql: |-
      SELECT * FROM UNNEST(ARRAY<INT64>[1, 2, 3, 3, 4]) AS number
      |> EXCEPT DISTINCT
      (
        SELECT * FROM UNNEST(ARRAY<INT64>[1, 2]) AS number
        |> EXCEPT DISTINCT
            (SELECT * FROM UNNEST(ARRAY<INT64>[1, 4]) AS number)
      )
    expected:
      rows:
        - [1]
        - [3]
        - [4]
      unordered: true
  - desc: "pipe-syntax#49 L2229 WITH` pipe operator"
    sql: |-
      SELECT 1 AS key
      |> WITH t AS (
          SELECT 1 AS key, 'my_value' AS value
        )
      |> INNER JOIN t USING (key)
    expected:
      rows:
        - [1,"my_value"]
  - desc: "pipe-syntax#50 L2243 WITH` pipe operator"
    sql: |-
      SELECT 1 AS key
      -- Define multiple CTEs.
      |> WITH t1 AS (
          SELECT 2
        ), t2 AS (
          SELECT 3
        )
      |> UNION ALL (FROM t1), (FROM t2)
    expected:
      rows:
        - [1]
        - [2]
        - [3]
      unordered: true
  - desc: "pipe-syntax#51 L2264 WITH` pipe operator"
    sql: |-
      SELECT 1 AS key
      |> WITH t1 AS (
           SELECT 2
         ), t2 AS (
           SELECT 3
         ),
      |> UNION ALL (FROM t1), (FROM t2)
    expected:
      rows:
        - [1]
        - [2]
        - [3]
      unordered: true
  - desc: "pipe-syntax#52 L2284 WITH` pipe operator"
    sql: |-
      SELECT 1 AS key
      |> WITH RECURSIVE t AS (
          SELECT 2 AS key
          |> UNION ALL (
            SELECT key + 1 AS key
            FROM t
            WHERE key <= 3
          )
        )
      |> UNION ALL (FROM t)
    expected:
      rows:
        - [1]
        - [2]
        - [3]
        - [4]
      unordered: true
  - desc: "pipe-syntax#53 L2334 PIVOT` pipe operator"
    sql: |-
      (
        SELECT "kale" AS product, 51 AS sales, "Q1" AS quarter
        UNION ALL
        SELECT "kale" AS product, 4 AS sales, "Q1" AS quarter
        UNION ALL
        SELECT "kale" AS product, 45 AS sales, "Q2" AS quarter
        UNION ALL
        SELECT "apple" AS product, 8 AS sales, "Q1" AS quarter
        UNION ALL
        SELECT "apple" AS product, 10 AS sales, "Q2" AS quarter
      )
      |> PIVOT(SUM(sales) FOR quarter IN ('Q1', 'Q2'))
    expected:
      rows:
        - ["kale",55,45]
        - ["apple",8,10]
      unordered: true
  - desc: "pipe-syntax#54 L2361 PIVOT` pipe operator"
    sql: |-
      (
        SELECT "kale" AS product, 51 AS sales, "Q1" AS quarter
        UNION ALL
        SELECT "kale" AS product, 4 AS sales, "Q1" AS quarter
        UNION ALL
        SELECT "kale" AS product, 45 AS sales, "Q2" AS quarter
        UNION ALL
        SELECT "apple" AS product, 8 AS sales, "Q1" AS quarter
        UNION ALL
        SELECT "apple" AS product, 10 AS sales, "Q2" AS quarter
      )
      |> PIVOT(SUM(sales) FOR quarter IN ('Q1' AS first_quarter, 'Q2' AS second_quarter)) AS pivoted_sales
      |> SELECT pivoted_sales.*
    expected:
      rows:
        - ["kale",55,45]
        - ["apple",8,10]
      unordered: true
  - desc: "pipe-syntax#55 L2400 UNPIVOT` pipe operator"
    sql: |-
      (
        SELECT 'kale' as product, 55 AS Q1, 45 AS Q2
        UNION ALL
        SELECT 'apple', 8, 10
      )
      |> UNPIVOT(sales FOR quarter IN (Q1, Q2))
    expected:
      rows:
        - ["kale",55,"Q1"]
        - ["kale",45,"Q2"]
        - ["apple",8,"Q1"]
        - ["apple",10,"Q2"]
      unordered: true
  - desc: "pipe-syntax#56 L2445 WINDOW` pipe operator (DEPRECATED)"
    sql: |-
      (
        SELECT 'apples' AS item, 2 AS sales
        UNION ALL
        SELECT 'bananas' AS item, 5 AS sales
        UNION ALL
        SELECT 'carrots' AS item, 8 AS sales
      )
      |> WINDOW SUM(sales) OVER() AS total_sales
    expected:
      rows:
        - ["apples",2,15]
        - ["bananas",5,15]
        - ["carrots",8,15]
      unordered: true
  - desc: "pipe-syntax#58 L2616 DESCRIBE` pipe operator"
    sql: |-
      -- Baseline example without DESCRIBE.

      FROM
        (SELECT 1 AS col1, 2 AS col2) AS table1,
        (SELECT 'abc' AS col3, 1.0, 123 AS col5) AS table2
      |> EXTEND col1 + col5 AS computed, 'anonymous column'
    expected:
      rows:
        - [1,2,"abc",1,123,124,"anonymous column"]
  - desc: "pipe-syntax#59 L2634 DESCRIBE` pipe operator"
    skip: "not_in_bigquery: Pipe DESCRIBE; BigQuery raises \"Pipe DESCRIBE not supported\"."
    sql: |-
      -- Same example with DESCRIBE to describe table aliases and columns.

      FROM
        (SELECT 1 AS col1, 2 AS col2) AS table1,
        (SELECT 'abc' AS col3, 1.0, 123 AS col5) AS table2
      |> EXTEND col1 + col5 AS computed, 'anonymous column'
      |> DESCRIBE
    expected:
      rows:
        - ["**Columns**:"]
        - ["Table Alias  Column Name  Type"]
        - ["table1       col1         INT64"]
        - ["table1       col2         INT64"]
        - ["table2       col3         STRING"]
        - ["table2       \u003cunnamed\u003e    DOUBLE"]
        - ["table2       col5         INT64"]
        - ["computed     INT64"]
        - ["\u003cunnamed\u003e    STRING"]
        - [""]
        - ["**Table Aliases**:"]
        - ["Table Alias  Columns"]
        - ["table1       col1, col2"]
        - ["table2       col3, \u003cunnamed\u003e, col5"]
      unordered: true
  - desc: "pipe-syntax#60 L2667 DESCRIBE` pipe operator"
    skip: "not_in_bigquery: Pipe DESCRIBE; BigQuery raises \"Pipe DESCRIBE not supported\"."
    sql: |-
      -- Describe CTEs and ordering.

      WITH cte AS (SELECT 1 AS col1, 'abc' AS col2)
      FROM cte AS table1
      |> SELECT col1
      |> ORDER BY col1
      |> DESCRIBE
    expected:
      rows:
        - ["**Columns**:"]
        - ["Column Name  Type"]
        - ["col1         INT64"]
        - [""]
        - ["**Common table expressions**:"]
        - ["Name  Columns"]
        - ["cte   col1, col2"]
        - [""]
        - ["Result is ordered."]
  - desc: "pipe-syntax#61 L2695 DESCRIBE` pipe operator"
    skip: "not_in_bigquery: Pipe DESCRIBE; BigQuery raises \"Pipe DESCRIBE not supported\"."
    sql: |-
      -- Describe a value table.

      FROM (SELECT AS STRUCT 1 AS x, 'abc' AS y) AS value_table
      |> DESCRIBE
    expected:
      rows:
        - ["**Columns**:"]
        - ["Table Alias  Column Name  Type"]
        - ["value_table  \u003cvalue\u003e      STRUCT\u003cx INT64, y STRING\u003e"]
        - [""]
        - ["**Table Aliases**:"]
        - ["Table Alias  Columns"]
        - ["value_table  \u003cvalue\u003e"]
        - [""]
        - ["Result is a value table."]
      unordered: true
  - desc: "pipe-syntax#62 L2767 IF` pipe operator"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      FROM Produce
      |> IF true THEN
        (
          |> AGGREGATE COUNT(*) AS total
        ) ELSE
        (
          |> SELECT *  -- Never evaluated
        )
      |> AS Inventory
    expected:
      rows:
        - [4]
  - desc: "pipe-syntax#63 L2786 IF` pipe operator"
    setup:
      - |-
        CREATE OR REPLACE TABLE Produce AS (
          SELECT 'apples' AS item, 2 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'carrots' AS item, 8 AS sales, 'vegetable' AS category
          UNION ALL
          SELECT 'apples' AS item, 7 AS sales, 'fruit' AS category
          UNION ALL
          SELECT 'bananas' AS item, 5 AS sales, 'fruit' AS category
        )
    sql: |-
      FROM Produce
      -- No case is selected, causing IF to be no-op.
      |> IF false THEN
          (
            -- This doesn't raise an error, because the case isn't evaluated.
            |> SELECT non_existent_column
          )
      |> SELECT *
    expected:
      rows:
        - ["apples",2,"fruit"]
        - ["carrots",8,"vegetable"]
        - ["apples",7,"fruit"]
        - ["bananas",5,"fruit"]
      unordered: true
  - desc: "pipe-syntax#65 L3127 TEE` semi-terminal pipe operator"
    skip: "not_in_bigquery: Pipe TEE; BigQuery raises \"Pipe TEE not supported\"."
    sql: |-
      WITH
        Fruit AS (
          SELECT 'apple' AS item, 100 AS sales
          UNION ALL
          SELECT 'banana' AS item, 150 AS sales
        )
      FROM Fruit
      |> TEE
          (|> AGGREGATE COUNT(*) AS total_fruits),
          (|> AGGREGATE COUNT(*) AS count_by_item GROUP BY item)
      |> SELECT *
    expected:
      rows:
        - ["2"]
  - desc: "pipe-syntax#66 L3198 TEE` semi-terminal pipe operator"
    skip: "not_in_bigquery: Pipe TEE; BigQuery raises \"Pipe TEE not supported\"."
    sql: |-
      WITH
        Fruit AS (
          SELECT 'apple' AS item, 100 AS sales
          UNION ALL
          SELECT 'banana' AS item, 150 AS sales
        )
      FROM Fruit
      |> TEE
      |> TEE
          (|> AGGREGATE COUNT(*) AS num_rows)
      |> AGGREGATE SUM(sales) AS total_sales
    expected:
      rows:
        - ["apple","100"]
        - ["banana","150"]
      unordered: true
