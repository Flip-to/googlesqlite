# Generated by TestDocsExamples from google/googlesql@d82db99 docs/statistical_aggregate_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#1 L178 CORR"
    sql: |-
      SELECT CORR(y, x) AS results
      FROM
        UNNEST(
          [
            STRUCT(1.0 AS y, 5.0 AS x),
            (3.0, 9.0),
            (4.0, 7.0)])
    expected:
      rows:
        - [0.6546536707079772]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#2 L194 CORR"
    sql: |-
      SELECT CORR(y, x) AS results
      FROM
        UNNEST(
          [
            STRUCT(1.0 AS y, 5.0 AS x),
            (3.0, 9.0),
            (4.0, NULL)])
    expected:
      rows:
        - [1]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#3 L210 CORR"
    sql: |-
      SELECT CORR(y, x) AS results
      FROM UNNEST([STRUCT(1.0 AS y, NULL AS x),(9.0, 3.0)])
    expected:
      rows:
        - [null]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#4 L221 CORR"
    sql: |-
      SELECT CORR(y, x) AS results
      FROM UNNEST([STRUCT(1.0 AS y, NULL AS x),(9.0, NULL)])
    expected:
      rows:
        - [null]
  - desc: "statistical_aggregate_functions#5 L232 CORR"
    sql: |-
      SELECT CORR(y, x) AS results
      FROM
        UNNEST(
          [
            STRUCT(1.0 AS y, 5.0 AS x),
            (3.0, 9.0),
            (4.0, 7.0),
            (5.0, 1.0),
            (7.0, CAST('Infinity' as DOUBLE))])
    expected:
      rows:
        - ["NaN"]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#6 L250 CORR"
    sql: |-
      SELECT CORR(x, y) AS results
      FROM
        (
          SELECT 0 AS x, 0 AS y
          UNION ALL
          SELECT 0 AS x, 0 AS y
        )
    expected:
      rows:
        - ["NaN"]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#7 L337 COVAR_POP"
    sql: |-
      SELECT COVAR_POP(y, x) AS results
      FROM
        UNNEST(
          [
            STRUCT(1.0 AS y, 1.0 AS x),
            (2.0, 6.0),
            (9.0, 3.0),
            (2.0, 6.0),
            (9.0, 3.0)])
    expected:
      rows:
        - [-1.6800000000000002]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#8 L355 COVAR_POP"
    sql: |-
      SELECT COVAR_POP(y, x) AS results
      FROM UNNEST([STRUCT(1.0 AS y, NULL AS x),(9.0, 3.0)])
    expected:
      rows:
        - [0]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#9 L366 COVAR_POP"
    sql: |-
      SELECT COVAR_POP(y, x) AS results
      FROM UNNEST([STRUCT(1.0 AS y, NULL AS x),(9.0, NULL)])
    expected:
      rows:
        - [null]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#10 L377 COVAR_POP"
    sql: |-
      SELECT COVAR_POP(y, x) AS results
      FROM
        UNNEST(
          [
            STRUCT(1.0 AS y, 1.0 AS x),
            (2.0, 6.0),
            (9.0, 3.0),
            (2.0, 6.0),
            (NULL, 3.0)])
    expected:
      rows:
        - [-1]
  - desc: "statistical_aggregate_functions#11 L395 COVAR_POP"
    sql: |-
      SELECT COVAR_POP(y, x) AS results
      FROM
        UNNEST(
          [
            STRUCT(1.0 AS y, 1.0 AS x),
            (2.0, 6.0),
            (9.0, 3.0),
            (2.0, 6.0),
            (CAST('Infinity' as DOUBLE), 3.0)])
    expected:
      rows:
        - ["NaN"]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#12 L488 COVAR_SAMP"
    sql: |-
      SELECT COVAR_SAMP(y, x) AS results
      FROM
        UNNEST(
          [
            STRUCT(1.0 AS y, 1.0 AS x),
            (2.0, 6.0),
            (9.0, 3.0),
            (2.0, 6.0),
            (9.0, 3.0)])
    expected:
      rows:
        - [-2.1]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#14 L524 COVAR_SAMP"
    sql: |-
      SELECT COVAR_SAMP(y, x) AS results
      FROM UNNEST([STRUCT(1.0 AS y, NULL AS x),(9.0, 3.0)])
    expected:
      rows:
        - [null]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#15 L535 COVAR_SAMP"
    sql: |-
      SELECT COVAR_SAMP(y, x) AS results
      FROM UNNEST([STRUCT(1.0 AS y, NULL AS x),(9.0, NULL)])
    expected:
      rows:
        - [null]
  - desc: "statistical_aggregate_functions#16 L546 COVAR_SAMP"
    sql: |-
      SELECT COVAR_SAMP(y, x) AS results
      FROM
        UNNEST(
          [
            STRUCT(1.0 AS y, 1.0 AS x),
            (2.0, 6.0),
            (9.0, 3.0),
            (2.0, 6.0),
            (CAST('Infinity' as DOUBLE), 3.0)])
    expected:
      rows:
        - ["NaN"]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#17 L669 STDDEV_POP"
    sql: |-
      SELECT STDDEV_POP(x) AS results FROM UNNEST([10, 14, 18]) AS x
    expected:
      rows:
        - [3.265986323710904]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#18 L679 STDDEV_POP"
    sql: |-
      SELECT STDDEV_POP(x) AS results FROM UNNEST([10, 14, NULL]) AS x
    expected:
      rows:
        - [2]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#19 L689 STDDEV_POP"
    sql: |-
      SELECT STDDEV_POP(x) AS results FROM UNNEST([10, NULL]) AS x
    expected:
      rows:
        - [0]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#20 L699 STDDEV_POP"
    sql: |-
      SELECT STDDEV_POP(x) AS results FROM UNNEST([NULL]) AS x
    expected:
      rows:
        - [null]
  - desc: "statistical_aggregate_functions#21 L709 STDDEV_POP"
    sql: |-
      SELECT STDDEV_POP(x) AS results FROM UNNEST([10, 14, CAST('Infinity' as DOUBLE)]) AS x
    expected:
      rows:
        - ["NaN"]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#22 L792 STDDEV_SAMP"
    sql: |-
      SELECT STDDEV_SAMP(x) AS results FROM UNNEST([10, 14, 18]) AS x
    expected:
      rows:
        - [4]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#23 L802 STDDEV_SAMP"
    sql: |-
      SELECT STDDEV_SAMP(x) AS results FROM UNNEST([10, 14, NULL]) AS x
    expected:
      rows:
        - [2.8284271247461903]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#24 L812 STDDEV_SAMP"
    sql: |-
      SELECT STDDEV_SAMP(x) AS results FROM UNNEST([10, NULL]) AS x
    expected:
      rows:
        - [null]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#25 L822 STDDEV_SAMP"
    sql: |-
      SELECT STDDEV_SAMP(x) AS results FROM UNNEST([NULL]) AS x
    expected:
      rows:
        - [null]
  - desc: "statistical_aggregate_functions#26 L832 STDDEV_SAMP"
    sql: |-
      SELECT STDDEV_SAMP(x) AS results FROM UNNEST([10, 14, CAST('Infinity' as DOUBLE)]) AS x
    expected:
      rows:
        - ["NaN"]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#27 L902 VAR_POP"
    sql: |-
      SELECT VAR_POP(x) AS results FROM UNNEST([10, 14, 18]) AS x
    expected:
      rows:
        - [10.666666666666666]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#28 L912 VAR_POP"
    sql: |-
      SELECT VAR_POP(x) AS results FROM UNNEST([10, 14, NULL]) AS x
    expected:
      rows:
        - [4]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#29 L922 VAR_POP"
    sql: |-
      SELECT VAR_POP(x) AS results FROM UNNEST([10, NULL]) AS x
    expected:
      rows:
        - [0]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#30 L932 VAR_POP"
    sql: |-
      SELECT VAR_POP(x) AS results FROM UNNEST([NULL]) AS x
    expected:
      rows:
        - [null]
  - desc: "statistical_aggregate_functions#31 L942 VAR_POP"
    sql: |-
      SELECT VAR_POP(x) AS results FROM UNNEST([10, 14, CAST('Infinity' as DOUBLE)]) AS x
    expected:
      rows:
        - ["NaN"]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#32 L1025 VAR_SAMP"
    sql: |-
      SELECT VAR_SAMP(x) AS results FROM UNNEST([10, 14, 18]) AS x
    expected:
      rows:
        - [16]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#33 L1035 VAR_SAMP"
    sql: |-
      SELECT VAR_SAMP(x) AS results FROM UNNEST([10, 14, NULL]) AS x
    expected:
      rows:
        - [8]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#34 L1045 VAR_SAMP"
    sql: |-
      SELECT VAR_SAMP(x) AS results FROM UNNEST([10, NULL]) AS x
    expected:
      rows:
        - [null]
  # Verified on BigQuery 2026-09-25.
  - desc: "statistical_aggregate_functions#35 L1055 VAR_SAMP"
    sql: |-
      SELECT VAR_SAMP(x) AS results FROM UNNEST([NULL]) AS x
    expected:
      rows:
        - [null]
  - desc: "statistical_aggregate_functions#36 L1065 VAR_SAMP"
    sql: |-
      SELECT VAR_SAMP(x) AS results FROM UNNEST([10, 14, CAST('Infinity' as DOUBLE)]) AS x
    expected:
      rows:
        - ["NaN"]
