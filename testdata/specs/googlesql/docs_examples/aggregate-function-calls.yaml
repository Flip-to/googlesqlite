# Generated by TestDocsExamples from google/googlesql@d82db99 docs/aggregate-function-calls.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "aggregate-function-calls#5 L271 Aggregate function examples"
    sql: |-
      SELECT
        COUNT(*) AS total_count,
        COUNT(fruit) AS non_null_count,
        MIN(fruit) AS min,
        MAX(fruit) AS max
      FROM
        (
          SELECT NULL AS fruit
          UNION ALL
          SELECT 'apple' AS fruit
          UNION ALL
          SELECT 'pear' AS fruit
          UNION ALL
          SELECT 'orange' AS fruit
        )
    expected:
      rows:
        - [4,3,"apple","pear"]
  - desc: "aggregate-function-calls#6 L299 Aggregate function examples"
    sql: |-
      SELECT
        x,
        AVG(x) OVER (ORDER BY x ROWS BETWEEN 1 PRECEDING AND CURRENT ROW) AS avg
      FROM UNNEST([0, 2, 4, 4, 5]) AS x
    expected:
      rows:
        - [0,0]
        - [2,1]
        - [4,3]
        - [4,4]
        - [5,4.5]
      unordered: true
