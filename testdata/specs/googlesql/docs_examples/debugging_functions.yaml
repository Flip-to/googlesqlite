# Generated by TestDocsExamples from google/googlesql@d82db99 docs/debugging_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "debugging_functions#1 L88 ERROR"
    sql: |-
      -- ERROR: Show this error message (while evaluating error("Show this error message"))
      SELECT ERROR('Show this error message')
    expected:
      error:
        contains: ""
  - desc: "debugging_functions#2 L96 ERROR"
    sql: |-
      SELECT
        CASE
          WHEN value = 'foo' THEN 'Value is foo.'
          WHEN value = 'bar' THEN 'Value is bar.'
          ELSE ERROR(CONCAT('Found unexpected value: ', value))
        END AS new_value
      FROM (
        SELECT 'foo' AS value UNION ALL
        SELECT 'bar' AS value UNION ALL
        SELECT 'baz' AS value)
    expected:
      error:
        contains: ""
  - desc: "debugging_functions#3 L127 ERROR"
    sql: |-
      SELECT *
      FROM (SELECT -1 AS x)
      WHERE IF(x > 0, true, ERROR(FORMAT('Error: x must be positive but is %t', x)))
    expected:
      error:
        contains: ""
  - desc: "debugging_functions#11 L271 IFERROR"
    sql: |-
      SELECT IFERROR(ERROR('a'), ERROR('b')) AS result
    expected:
      error:
        contains: ""
