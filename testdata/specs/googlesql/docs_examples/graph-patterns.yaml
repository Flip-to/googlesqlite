# Generated by TestDocsExamples from google/googlesql@d82db99 docs/graph-patterns.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#22 L1271 Details"
    sql: |-
      -- ERROR: Minimum path length for the quantified path is 0.
      MATCH (a:Account){1, 3}
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#23 L1276 Details"
    sql: |-
      -- ERROR: Minimum node count and minimum path length for the entire path is 0.
      MATCH ((a:Account)-[t:Transfers]->(b:Account)){0}
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#24 L1281 Details"
    sql: |-
      -- ERROR: Minimum path length for the entire path is 0 when quantified portion
      -- is repeated 0 times.
      MATCH (:Person)((a:Account)-[t:Transfers]->(b:Account)){0, 3}
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#25 L1287 Details"
    sql: |-
      -- ERROR: `a` is declared once as a group variable and once as a singleton
      -- variable.
      MATCH (c:Account) ((a:Account)-[t:Transfers]->(b:Account)){1, 3}->(a:Account)
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#26 L1293 Details"
    sql: |-
      -- ERROR: `a` is declared twice as a group variable.
      MATCH ((a:Account)-[t:Transfers]->(b:Account)){1, 3}-[u:Transfers]->((a:Account)-[v:Transfers]-(c:Account)){2}
    expected:
      error:
        contains: ""
  - desc: "graph-patterns#35 L1654 Examples"
    skip: "not_in_bigquery: GQL graph query over the FinGraph property graph."
    sql: |-
      GRAPH FinGraph
      MATCH ALL (a:Account {id: 20})-[t:Transfers]->(b:Account)
      RETURN a.id AS a_id, t.amount, b.id AS b_id

      -- Equivalent:
      GRAPH FinGraph
      MATCH (a:Account {id: 20})-[t:Transfers]->(b:Account)
      RETURN a.id AS a_id, t.amount, b.id AS b_id
    expected:
      rows:
        - ["20","500","7"]
        - ["20","200","16"]
      unordered: true
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#37 L1692 Examples"
    sql: |-
      -- Error
      GRAPH FinGraph
      MATCH
        ANY (a:Account {id: 20})->(mid:Account)->(a:Account)->(mid:Account)->(a:Account),
        ALL (p:Person)->(mid)
      RETURN p.name
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#40 L1742 Examples"
    sql: |-
      -- Error
      GRAPH FinGraph
      MATCH ANY ((p:Person)->(f:Person)){1, 3},
            ALL ->(p)->
      RETURN p.name
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#41 L1750 Examples"
    sql: |-
      -- Error
      GRAPH FinGraph
      MATCH ANY ((p:Person)->(f:Person)){1, 3},
            ALL ((p)->){1, 3}
      RETURN p.name
    expected:
      error:
        contains: ""
  # Verified on BigQuery 2026-09-25.
  - desc: "graph-patterns#54 L2052 Examples"
    sql: |-
      -- Error
      GRAPH FinGraph
      MATCH ANY SHORTEST TRAIL ->{1,4}
      RETURN COUNT(1) as num_paths
    expected:
      error:
        contains: ""
