# Generated by TestDocsExamples from google/googlesql@d82db99 docs/data-definition-language.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   data-definition-language#2: error (query failed): failed to analyze: Unrecognized name: DEFAULT_HEIGHT [at 1:9]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "data-definition-language#2 L852 CREATE CONSTANT"
    sql: |-
      SELECT (DEFAULT_HEIGHT + 5) AS result
    expected:
      rows:
        - ["30"]
