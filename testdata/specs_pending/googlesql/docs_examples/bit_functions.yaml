# Generated by TestDocsExamples from google/googlesql@d82db99 docs/bit_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   bit_functions#2: fail (values differ): driver rows [[-1 -1]]
#   bit_functions#4: fail (values differ): driver rows [[-1 -1]]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "bit_functions#2 L154 BIT_CAST_TO_INT64"
    sql: |-
      SELECT BIT_CAST_TO_UINT64(-1) as UINT64_value, BIT_CAST_TO_INT64(BIT_CAST_TO_UINT64(-1)) as bit_cast_value
    expected:
      rows:
        - ["18446744073709551615",-1]
  - desc: "bit_functions#4 L220 BIT_CAST_TO_UINT64"
    sql: |-
      SELECT -1 as INT64_value, BIT_CAST_TO_UINT64(-1) as bit_cast_value
    expected:
      rows:
        - [-1,"18446744073709551615"]
