# Generated by TestDocsExamples from google/googlesql@d82db99 docs/collation-concepts.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   collation-concepts#1: fail (values differ): driver rows [[a] [b] [b]]
#   collation-concepts#3: fail (same rows, different order under ORDER BY (may be ties in the sort key)): driver rows [[a12b] [a1b]]
#   collation-concepts#5: fail (values differ): driver rows [[orange3] [oran⁠ge1] [⁠orange2]]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "collation-concepts#1 L70 Operations that propagate collation"
    sql: |-
      -- With collation
      SELECT *
      FROM UNNEST([
        COLLATE('B', 'und:ci'),
        'b',
        'a'
      ]) AS character
      ORDER BY character
    expected:
      rows:
        - ["a"]
        - ["B"]
        - ["b"]
  - desc: "collation-concepts#3 L574 Extensions"
    sql: |-
      SELECT *
      FROM UNNEST([
        'a12b',
        'a1b'
      ]) AS ids
      ORDER BY ids COLLATE 'en-us-u-kn-true'
    expected:
      rows:
        - ["a1b"]
        - ["a12b"]
  - desc: "collation-concepts#5 L662 Caveats"
    sql: |-
      SELECT *
        FROM UNNEST([
          COLLATE('oran\u2060ge1', 'und:ci'),
          COLLATE('\u2060orange2', 'und:ci'),
          COLLATE('orange3', 'und:ci')
        ]) AS fruit
        ORDER BY fruit
    expected:
      rows:
        - ["orange1"]
        - ["orange2"]
        - ["orange3"]
