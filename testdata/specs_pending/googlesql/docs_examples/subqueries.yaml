# Generated by TestDocsExamples from google/googlesql@d82db99 docs/subqueries.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   subqueries#5: error (query failed): failed to analyze: The LIKE ANY|SOME|ALL operator does not support subquery expression as patterns. Patterns must be string or bytes; did you mean LIKE ANY (pattern1, pattern2, ...)? [at 5:25]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "subqueries#5 L265 Quantified `LIKE` subqueries"
    sql: |-
      WITH Words AS (
        SELECT '%orb%' as chars UNION ALL
        SELECT 'xyg'
      )
      SELECT 'corba' LIKE ANY (SELECT chars FROM Words) as result
    expected:
      rows:
        - ["TRUE"]
