# Generated by TestDocsExamples from google/googlesql@d82db99 docs/json_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   json_functions#469: fail (values differ): driver rows [[NULL]]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "json_functions#469 L8482 Non-validation of `STRING` inputs"
    pending: "driver_bug: JSON_VALUE on a STRING input does not validate the whole document: JSON_VALUE('{\"hello\": \"world\"', \"$.hello\") is 'world' on BigQuery (verified); the driver parses the full text and returns NULL."
    sql: |-
      SELECT JSON_VALUE('{"hello": "world"', "$.hello") AS hello
    expected:
      rows:
        - ["world"]
