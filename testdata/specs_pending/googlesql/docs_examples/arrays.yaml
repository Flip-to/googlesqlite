# Generated by TestDocsExamples from google/googlesql@d82db99 docs/arrays.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   arrays#2: error (query failed): failed to analyze: Array element access with array[position] is not supported. Use array[OFFSET(zero_based_offset)] or array[ORDINAL(one_based_ordinal)] [at 3:16]
#   arrays#14: error (query failed): failed to parse statements: failed to parse statement: Syntax error: Array element access is not allowed in the FROM clause without UNNEST; Use UNNEST(<expression>) [at 4:41]
#   arrays#27: error (query failed): failed to analyze: Casting between arrays with incompatible element types is not supported: Invalid cast from ARRAY<INT32> to ARRAY<DOUBLE> [at 1:13]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "arrays#2 L57 Accessing array elements"
    setup:
      - |-
        CREATE TABLE Sequences AS SELECT [0, 1, 1, 2, 3, 5] AS some_numbers UNION ALL
            SELECT [2, 4, 8, 16, 32] UNION ALL
            SELECT [5, 10]
    sql: |-
      SELECT
        some_numbers,
        some_numbers[0] AS index_0,
        some_numbers[OFFSET(1)] AS offset_1,
        some_numbers[ORDINAL(1)] AS ordinal_1
      FROM Sequences
    expected:
      rows:
        - ["[0, 1, 1, 2, 3, 5]","0","1","0"]
        - ["[2, 4, 8, 16, 32]","2","4","2"]
        - ["[5, 10]","5","10","5"]
      unordered: true
  - desc: "arrays#14 L354 Flattening nested data into a table"
    setup:
      - |-
        CREATE TABLE ItemsTable AS SELECT [
          STRUCT('red' AS color,
                 2 AS inventory,
                 [STRUCT('a' AS agent, [100.0, 50.0] AS prices),
                  STRUCT('c' AS agent, [25.0] AS prices)] AS sales),
          STRUCT('green' AS color,
                 NULL AS inventory,
                 [STRUCT('a' AS agent, [75.0] AS prices),
                  STRUCT('b' AS agent, [200.0] AS prices)] AS sales),
          STRUCT('orange' AS color,
                 10 AS inventory,
                 NULL AS sales)
        ] AS items
      - |-
        -- In UNNEST (FLATTEN used explicitly):
        SELECT second_prices
        FROM ItemsTable
        INNER JOIN UNNEST(FLATTEN(items.sales.prices[SAFE_OFFSET(1)])) AS second_prices
      - |-
        -- In UNNEST (FLATTEN used implicitly):
        SELECT second_prices
        FROM ItemsTable
        INNER JOIN UNNEST(items.sales.prices[SAFE_OFFSET(1)]) AS second_prices
    sql: |-
      -- In the FROM clause (UNNEST used implicitly):
      SELECT second_prices
      FROM ItemsTable
      INNER JOIN ItemsTable.items.sales.prices[SAFE_OFFSET(1)] AS second_prices
    expected:
      rows:
        - ["50"]
        - [null]
        - [null]
        - [null]
      unordered: true
  - desc: "arrays#27 L899 Casting arrays"
    sql: |-
      SELECT CAST(int_array AS ARRAY<DOUBLE>) AS double_array
      FROM (SELECT ARRAY<INT32>[1, 2, 3] AS int_array)
    expected:
      rows:
        - ["[1, 2, 3]"]
