# Generated by TestDocsExamples from google/googlesql@d82db99 docs/lexical.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   lexical#2: error (query failed): failed to analyze: Concatenation of subsequent string literals is not supported. Did you mean to use the || operator? [at 2:16]
#   lexical#3: error (query failed): failed to analyze: Concatenation of subsequent string literals is not supported. Did you mean to use the || operator? [at 2:16]
#   lexical#4: error (query failed): failed to analyze: Concatenation of subsequent bytes literals is not supported. Did you mean to use the || operator? [at 2:18]
#   lexical#5: error (query failed): failed to analyze: Concatenation of subsequent bytes literals is not supported. Did you mean to use the || operator? [at 3:18]
#   lexical#6: fail (row count differs: docs 0, driver 1): driver rows [[{"name":"my_file.md","regex":"\\d+"}]]
#   lexical#7: fail (row count differs: docs 0, driver 1): driver rows [[-1.2]]
#   lexical#8: fail (row count differs: docs 0, driver 1): driver rows [[2014-01-31]]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "lexical#2 L271 Tokens in literals"
    sql: |-
      -- Compatible format types can be used together in a string literal.
        SELECT 'abc' "d" '''ef'''
    expected:
      rows: []
  - desc: "lexical#3 L280 Tokens in literals"
    sql: |-
      -- \n is escaped in the raw string token but not in the quoted string token.
        SELECT '\na' r"\n"
    expected:
      rows:
        - ["a\\n"]
  - desc: "lexical#4 L301 Tokens in literals"
    sql: |-
      -- Compatible format types can be used together in a bytes literal.
        SELECT b'\x41' b'''\x42''' b"""\x41"""
    expected:
      rows: []
  - desc: "lexical#5 L310 Tokens in literals"
    sql: |-
      -- Control characters are escaped in the raw bytes tokens but not in the
        -- bytes token.
        SELECT b'\x41' RB'\x42' br'\x41'
    expected:
      rows: []
  - desc: "lexical#6 L322 Tokens in literals"
    sql: |-
      -- The following JSON literal is equivalent to: JSON '{"name":"my_file.md","regex":"\\d+"}'
      SELECT JSON '{"name": "my_file.md", "regex": ' /*start*/ r' "\\d+"' /*end*/ '}'
    expected:
      rows: []
  - desc: "lexical#7 L331 Tokens in literals"
    sql: |-
      -- The following NUMERIC literal is equivalent to: NUMERIC '-1.2'
      SELECT NUMERIC '-' "1" '''.''' r'2'
    expected:
      rows: []
  - desc: "lexical#8 L349 Tokens in literals"
    sql: |-
      -- The following DATE literal is equivalent to: DATE '2014-01-31'
      SELECT DATE /* year */ '2014' /* month and day */ "-01-31"
    expected:
      rows: []
