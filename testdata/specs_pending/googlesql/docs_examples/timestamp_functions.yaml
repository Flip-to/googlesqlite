# Generated by TestDocsExamples from google/googlesql@d82db99 docs/timestamp_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   timestamp_functions#38: fail (the driver does not match the BigQuery answer): driver rows [[1]]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "timestamp_functions#38 L1330 UNIX_MICROS"
    pending: "bigquery: timestamp_functions.md, UNIX_MICROS, documents UNIX_MICROS(TIMESTAMP \"1970-01-01 00:00:00.0000018+00\") as 1. BigQuery keeps microsecond precision and rejects the seven-digit fraction: \"Invalid TIMESTAMP literal\". The driver accepts nanosecond literals (FEATURE_TIMESTAMP_NANOS is on), so the case stays pending. (the driver does not match the BigQuery answer yet)"
    sql: |-
      SELECT UNIX_MICROS(TIMESTAMP "1970-01-01 00:00:00.0000018+00") AS micros
    expected:
      rows:
        - [1]
