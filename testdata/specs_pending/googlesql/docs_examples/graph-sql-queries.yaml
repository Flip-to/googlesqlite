# Generated by TestDocsExamples from google/googlesql@d82db99 docs/graph-sql-queries.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   graph-sql-queries#1: error (query failed): failed to parse statements: failed to parse statement: Syntax error: Expected ")" but got keyword MATCH [at 4:3]
#   graph-sql-queries#2: error (query failed): failed to parse statements: failed to parse statement: Syntax error: Expected ")" but got keyword MATCH [at 4:3]
#   graph-sql-queries#5: error (query failed): failed to parse statements: failed to parse statement: Syntax error: Expected ")" but got keyword MATCH [at 4:3]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "graph-sql-queries#1 L71 Examples"
    sql: |-
      SELECT name, id
      FROM GRAPH_TABLE(
        FinGraph
        MATCH (n:Person)
        RETURN n.name AS name, n.id AS id
      )
    expected:
      rows:
        - ["Alex","1"]
        - ["Dana","2"]
        - ["Lee","3"]
      unordered: true
  - desc: "graph-sql-queries#2 L91 Examples"
    sql: |-
      SELECT n.name, n.id
      FROM GRAPH_TABLE(
        FinGraph
        MATCH (n:Person)
        RETURN n
      )
    expected:
      rows:
        - ["Alex","1"]
        - ["Dana","2"]
        - ["Lee","3"]
      unordered: true
  - desc: "graph-sql-queries#5 L136 Examples"
    sql: |-
      SELECT SAFE_TO_JSON(n) as json_node
      FROM GRAPH_TABLE(
        FinGraph
        MATCH (n:Person)
        RETURN n
      )
    expected:
      rows:
        - ["{\"identifier\":\"mUZpbk...}"]
        - ["{\"identifier\":\"mUZpbk...}"]
        - ["{\"identifier\":\"mUZpbk...}"]
      unordered: true
