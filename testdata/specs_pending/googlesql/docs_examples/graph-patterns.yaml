# Generated by TestDocsExamples from google/googlesql@d82db99 docs/graph-patterns.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   graph-patterns#35: error (query failed): failed to parse statements: failed to parse statement: Syntax error: Expected ";" or end of input but got keyword GRAPH [at 6:1]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "graph-patterns#35 L1654 Examples"
    sql: |-
      GRAPH FinGraph
      MATCH ALL (a:Account {id: 20})-[t:Transfers]->(b:Account)
      RETURN a.id AS a_id, t.amount, b.id AS b_id

      -- Equivalent:
      GRAPH FinGraph
      MATCH (a:Account {id: 20})-[t:Transfers]->(b:Account)
      RETURN a.id AS a_id, t.amount, b.id AS b_id
    expected:
      rows:
        - ["20","500","7"]
        - ["20","200","16"]
      unordered: true
