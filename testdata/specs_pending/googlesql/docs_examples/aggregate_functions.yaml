# Generated by TestDocsExamples from google/googlesql@d82db99 docs/aggregate_functions.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports, except where a case says it
# follows BigQuery (see testdata/docs_examples/classification.yaml).
# Cases with `skip:` keep the documented expectation but do not run.
# Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   aggregate_functions#29: error (query failed): failed to analyze: WHERE modifier is not supported in function calls [at 1:25]
#   aggregate_functions#30: error (query failed): failed to analyze: WHERE modifier is not supported in function calls [at 15:29]
#   aggregate_functions#31: error (query failed): failed to analyze: WHERE modifier is not supported in function calls [at 11:21]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "aggregate_functions#29 L1339 COUNT"
    pending: "analyzer: Aggregate WHERE modifier; see aggregate-function-calls#7 (the go-googlesql v0.4.0 binding has no where_expr accessor)."
    sql: |-
      SELECT COUNT(DISTINCT x WHERE x > 0) AS distinct_positive
      FROM UNNEST([1, -2, 4, 1, -5, 4, 1, 3, -6, 1]) AS x
    expected:
      rows:
        - ["3"]
  - desc: "aggregate_functions#30 L1353 COUNT"
    pending: "analyzer: Aggregate WHERE modifier; see aggregate-function-calls#7 (the go-googlesql v0.4.0 binding has no where_expr accessor)."
    sql: |-
      WITH Events AS (
        SELECT DATE '2021-01-01' AS event_date, 'SUCCESS' AS event_type
        UNION ALL
        SELECT DATE '2021-01-02' AS event_date, 'SUCCESS' AS event_type
        UNION ALL
        SELECT DATE '2021-01-02' AS event_date, 'FAILURE' AS event_type
        UNION ALL
        SELECT DATE '2021-01-03' AS event_date, 'SUCCESS' AS event_type
        UNION ALL
        SELECT DATE '2021-01-04' AS event_date, 'FAILURE' AS event_type
        UNION ALL
        SELECT DATE '2021-01-04' AS event_date, 'FAILURE' AS event_type
      )
      SELECT
        COUNT(DISTINCT event_date WHERE event_type = 'FAILURE')
          AS distinct_dates_with_failures
      FROM Events
    expected:
      rows:
        - ["2"]
  - desc: "aggregate_functions#31 L1382 COUNT"
    pending: "analyzer: Aggregate WHERE modifier; see aggregate-function-calls#7 (the go-googlesql v0.4.0 binding has no where_expr accessor)."
    sql: |-
      WITH
        customers AS (
          SELECT 1934 AS id, 'a' AS team UNION ALL
          SELECT 2991, 'b' UNION ALL
          SELECT 3988, 'c'),
        vendors AS (
          SELECT 1934 AS id, 'd' AS team UNION ALL
          SELECT 2991, 'e' UNION ALL
          SELECT 4366, 'f')
      SELECT
        COUNT(DISTINCT id WHERE id IN (SELECT id FROM customers)) AS result
      FROM vendors
    expected:
      rows:
        - ["2"]
