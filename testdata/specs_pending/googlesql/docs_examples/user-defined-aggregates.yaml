# Generated by TestDocsExamples from google/googlesql@d82db99 docs/user-defined-aggregates.md.
# Expected values are the documented result tables, typed with the
# column types the analyzer reports. Do not edit by hand.
# PENDING: these cases diverge from the docs today. They live outside
# testdata/specs so the default suite stays green.
#   user-defined-aggregates#1: error (query failed): failed to analyze: Aggregate functions are not supported [at 1:1]
spec: docs/specs/googlesql/syntax/docs_examples.md
dialect: googlesql
cases:
  - desc: "user-defined-aggregates#1 L120 SQL UDA examples"
    sql: |-
      CREATE TEMP AGGREGATE FUNCTION ScaledSum(
        dividend DOUBLE,
        divisor DOUBLE NOT AGGREGATE)
      RETURNS DOUBLE
      AS (
        SUM(dividend) / divisor
      );
      SELECT ScaledSum(col1, 2) AS scaled_sum
      FROM (
        SELECT 1 AS col1 UNION ALL
        SELECT 3 AS col1 UNION ALL
        SELECT 5 AS col1
      )
    expected:
      rows:
        - ["4.5"]
